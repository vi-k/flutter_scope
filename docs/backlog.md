# Бэклог

> Записи владельца: что хочется сделать. Пишет владелец, агент читает,
> предлагает взять пункт в работу и удаляет выполненное (правила — в
> `AGENTS.md`, раздел 3). Состояние проекта — в `docs/handoff.md`.

- Нужно ли передавать в AsyncDataScope.builder полученную модель? В ScopeModel
  мы её не передаём.
- Нужен пример (и в README, и example) или шаблон создания скоупа для
  асинхронного контроллера: пример разделения UI и логики. Возможно, для этого
  нужно сделать отдельный `AsyncControllerScope`?
