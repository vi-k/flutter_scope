# Состояние проекта

> Обновлено: 2026-08-12. Файл поддерживается агентом и переписывается по ходу
> работы — это снимок «здесь и сейчас», а не история (история — в
> `docs/records/`) и не список пожеланий (он в `docs/backlog.md`).

## Где мы сейчас

Ветка `main`, рабочее дерево чистое, других веток и worktree нет.
Версия **0.10.0 подготовлена, но не опубликована** на pub.dev.

**2026-08-12 разобран технический долг** из этого файла и `TODO(nashol)` в
`async_scope_core.dart`. Логи в тестах стали opt-in (`SCOPO_LOG=debug` или
`logInit(level: …)`), поправлен `my_fake_async`, добавлены тесты на пустой
набор потоков в `init()` concurrent-группы и на ветку ошибки инициализации;
снят устаревший пункт про `test/notifier_test.dart` (файла нет с `2751810`);
`Stream.error` под тестовым биндингом закрыт как ограничение тестов, не дефект
(`docs/records/2026-08-12[1]-stream-error-under-test-binding.md`). По
`TODO(nashol)` найден и исправлен реальный дефект: провал отмены инициализации
бросал утилизацию, не сняв скоуп с учёта у родителя и не освободив `scopeKey`.
Тест-сьют вырос с 92 до 95.

**В работе:** фаза покрытия. Оба сценария из бэклога закрыты и удалены оттуда:
`notifyDependents` вместе с перестроением сверху (первые тесты слоя
`b_scope_widget`, `test/scope_widget_test.dart`) и переезд ребёнка с
`GlobalKey` под другого родителя (`test/async_scope_test.dart`). Дальше —
слои `c_scope_model`, `d_scope_notifier`, `f_async_data_scope`.

2026-08-12 настроен регламент работы агентов: точка входа `AGENTS.md`
(`CLAUDE.md` только импортирует её), этот файл, `docs/backlog.md`,
`docs/architecture.md`, история правок в `docs/records/`. Прежний `TODO.md`
разобран: пожелания владельца ушли в бэклог, технические проблемы — сюда.

Предыдущий эпизод — batch3, три оставшихся
известных дефекта, закрыты и смержены (`2ec6809`, отчёты в
`docs/records/2026-08-01[1]-cross-review-fixes.md`):

| Коммит | Что закрыто |
| --- | --- |
| `502c6ab` | Гонка инициализации: скоуп, ждущий `enter()` в очереди `scopeKey`, продолжал инициализацию после начала `close()` — в guard добавлен `_isDisposing` |
| `bee1d38` | `ScopeAutoDependencies`: `_root` не сбрасывался после `dispose` (ломалась повторная инициализация); ошибки групп терялись при disposal |
| `89d5070` | `LiteScope.close()` зависал на барьере скриншота — закрывающий кадр с `ScreenshotReplacer` выбрасывался оптимизацией `_shouldOnlyNotify` |

## Проверки

Прогон 2026-08-12: `fvm flutter test` — **95 тестов, все зелёные**; `analyze`
в обоих примерах чист, в корне — 2 замечания (см. ниже); `dart format` без
изменений, `dart doc` 0/0, `pub publish --dry-run` 0 предупреждений.

## Известные проблемы

- `fvm flutter analyze` в корне показывает 2 замечания:
  `avoid_classes_with_only_static_members` в `scope_config.dart:5` и
  `discarded_futures` в `screenshot_replacer.dart:105`. В отчёте batch3
  (2026-08-02) analyze значился чистым; замечания появились не в этой сессии —
  проверено на дереве без её изменений. Причина не установлена, оба файла с тех
  пор не менялись. Примеры чисты.
- Скоуп, чей `initAsync()`-генератор приостановлен на future, который никогда
  не завершится, невозможно отменить: `subscription.cancel()` не завершается, и
  утилизация не идёт дальше. Родитель выходит по своему
  `waitForChildrenTimeout`, но `scopeKey` такой скоуп не освобождает никогда.
  Найдено попутно при работе над отменой инициализации; отдельного теста нет.
- `dart format` расходится на 6 файлах `example/scopo_demo`: версия форматтера
  новее той, которой их писали, а переформатирование конфликтует с
  `require_trailing_commas`. Чинить вместе с обновлением тулчейна.
- Локи примеров писались более новым резолвером, чем тулчейн из `.fvmrc`
  (Flutter 3.29.2): любой `analyze`/`pub get` в `example/*` понижает в них
  версии зависимостей (например, `async` 2.13.1 → 2.12.0). Такие правки не
  коммитить — откатывать `git checkout -- example/`. Уйдёт вместе с
  обновлением тулчейна.
- Тестовое ограничение, не дефект: под `AutomatedTestWidgetsFlutterBinding`
  ошибка, лежащая в буфере потока до подписки (`Stream.error`), не проходит
  через `asyncMap` — а именно так подписывается `_performAsyncInit`. Ветку
  ошибки в тестах поднимать `async*`-потоком, который бросает после первого
  события; `Stream.error` — только внутри `tester.runAsync`. Разбор:
  `docs/records/2026-08-12[1]-stream-error-under-test-binding.md`.

## Пробелы в покрытии

Ноль собственных тестов у слоёв `a_base`, `b_scope_widget`, `c_scope_model`,
`d_scope_notifier`, `e_async_scope` (особенно `AsyncScopeCoordinator`),
`f_async_data_scope`, `g_lite_scope` — частично закрыты регрессионными тестами
0.10.0, но не системно. Отдельно не доказано освобождение `scopeKey` (мутанты
выживали при ревью batch3).

## Что дальше

Приоритет не выбран владельцем. Кандидаты, от наиболее готового к работе:

1. **Фаза покрытия** — тесты на слои с нулевым покрытием и два сценария из
   `docs/backlog.md` (`notifyDependents` + `setState`, перерегистрация ребёнка
   с `GlobalKey`).
2. **Документация** — 7 заглушек `doc/*.md`, категории dartdoc, скриншоты для
   pub.dev (см. `docs/backlog.md`).
3. **Публикация 0.10.0** — тесты, `dart doc` и `pub publish --dry-run` чистые;
   до публикации стоит закрыть 2 замечания `analyze` и решение о выпуске — за
   владельцем.
