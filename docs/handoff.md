# Состояние проекта

> Обновлено: 2026-08-16. Файл поддерживается агентом и переписывается по ходу
> работы — это снимок «здесь и сейчас», а не история (история — в
> `docs/records/`) и не список пожеланий (он в `docs/backlog.md`).

## С чего начать после рестарта

1. Прочитай этот файл целиком, потом `docs/backlog.md`.
2. Сверься: `git log --oneline -5`, `git status`.
3. Ветка `main`, других веток и worktree нет. В дереве не закоммичены две
   правки самого ревью: `docs/records/2026-08-16[12]-project-review.md` и этот
   файл. Кода ревью не касалось. Дальше — шесть P1, см. «Что дальше».

## Где мы сейчас

Версия **0.10.0 подготовлена, но не опубликована** на pub.dev. Владелец решил
исправлять все находки предвыпускного ревью по порядку и публиковать только
после закрытия всего списка, повторного полного ревью и явной просьбы.

**Полное независимое ревью перед публикацией** —
`docs/records/2026-08-14[10]-project-review.md` (выполненный план — `[9]`).
Подтверждено 18 находок: 0 P0, 3 P1, 12 P2, 3 P3.

**Закрыты все 18.** Список ревью исчерпан. После него закрыт и бэклог
владельца — последней записью в пакет добавилось **новое семейство
`AsyncControllerScope`** (скоуп, владеющий контроллером с собственным жизненным
циклом; дизайн, план и отчёт — записи `2026-08-16[9]`–`[11]`).

**Повторное полное ревью проведено** — `docs/records/2026-08-16[12]-project-review.md`.
Подтверждено 43 находки: **0 P0, 6 P1, 17 P2, 20 P3**. Код при ревью не
менялся. Публикация 0.10.0 не рекомендуется до закрытия шести P1.

Главный вывод ревью, важный для дальнейшей работы: **три P1 из шести — это тот
же дефект, что уже исправляли в 0.10.0, но в соседней ветке кода.** Падение
диспозера чинили в последовательной группе и не посмотрели в параллельную;
падение пользовательского хука закрывали в `disposeAsync` и не закрыли в
`onUnmount` десятью строками выше; девятку параметров пробросили в новом
семействе и не пробросили в двух старых. Отсюда правило на будущее: **закрыл
находку — найди её же в соседней реализации** и запиши результат поиска в
отчёт волны.

Отчёты по волнам, по одному на волну (восьмая — уже не по списку ревью;
девятая — новое семейство по бэклогу):

| волна | находки | отчёт |
| --- | --- | --- |
| 1 | P1 №3, P2 №4, №5 | `2026-08-16[1]-cleanup-completeness-report.md` |
| 2 | P2 №8, №9 | `2026-08-16[2]-scope-data-and-selectors-report.md` |
| 3 | P2 №11, №12, №13, P3 №18 | `2026-08-16[3]-listenable-utils-report.md` |
| 4 | P2 №6, №7, №10 | `2026-08-16[4]-lifecycle-contracts-report.md` |
| 5 | P2 №14, P3 №17 | `2026-08-16[5]-navigation-node-async-onpop-report.md` |
| 6 | P3 №16 | `2026-08-16[6]-scope-notifier-value-tag-report.md` |
| 7 | P2 №15 | `2026-08-16[7]-init-cancellation-timeout-report.md` |
| 8 | вне списка ревью | `2026-08-16[8]-dispose-async-timeout-report.md` |
| 9 | бэклог: `AsyncControllerScope` | `2026-08-16[11]-async-controller-scope-report.md` (дизайн — `[9]`, план — `[10]`) |

P1 №1 и P1 №2 закрыты раньше, вместе с ревью-волнами над ними: записи
`2026-08-15[1]`–`[3]`, `[5]`–`[10]`, `[12]`.

### Решения владельца, которые действуют дальше

- смена конструктора между владеющим и `.value` у `ScopeModel`/`ScopeNotifier`
  **запрещена ассертом**, а не переносится (волна 4);
- `State.dispose` **закрыт от переопределения** у `LiteScopeState`/`ScopeState`;
  синхронный разбор идёт в `onUnmount()` (волна 4);
- правило имён: **присваиваемый колбэк — короткий глагол** (`dep.unmount`,
  `AsyncScope(unmount:)`), **переопределяемый хук — `onX`**;
- скриншоты для pub.dev не добавляем: статичные кадры технического демо не
  объясняют ценность инфраструктурного пакета и не дают pub points;
- пакет не публиковать без явной просьбы.

## Проверки

Полный гейт из восьми команд `AGENTS.md` §6 пройден на закреплённом тулчейне
(Flutter 3.29.0 / Dart 3.7.0 через fvm, он же заявленный пол) на коммите
`9296648` — то есть **все находки повторного ревью получены на зелёном
гейте**: ни одна из них гейтом не ловится.

| проверка | результат |
| --- | --- |
| `fvm flutter test` | **222 теста, все зелёные** |
| `fvm flutter analyze` (корень) | `No issues found!` |
| `analyze` во всех трёх `example/*` | `No issues found!` в каждом |
| `fvm dart format --set-exit-if-changed lib test` | 84 файла, 0 changed |
| `fvm dart doc --dry-run` | 0 warnings, 0 errors |
| `fvm dart pub publish --dry-run` | 0 warnings |
| `sh docs/ru/check.sh` | переводы актуальны: 15 |

**В гейт §6 не входит:** у `example/scopo_demo` свои тесты (`fvm flutter test`
в его каталоге, 2 теста), и один из них импортирует папку демо по пути, так что
переименования его задевают.

Две ловушки самого гейта:

- `dart doc` и `pub publish` запускать **через `fvm dart`**, а не прямым бинарём
  из `~/fvm/versions/...`: без обёртки `dart doc` падает на
  `type 'Null' is not a subtype of type 'String'` в `DartdocOptionContext.sdkDir`;
- `dart format --set-exit-if-changed` **и пишет, и возвращает код**. Первый
  прогон после правок может вернуть 1, отформатировав файл; повторный даёт 0.
  Это не отказ гейта, но проверять надо повторным прогоном.

Тот же набор до перехода на 3.29.0 проходил и на глобальном 3.44.9.

## Грабли, на которые не стоит наступать второй раз

Найдено по ходу волн, стоило времени, в коде не видно:

- **внешний `catch` в `_performAsyncInit` выполняется внутри
  `performRebuild`.** Отказы, ради которых он существует (поиск координатора,
  не-генераторный `initAsync`), происходят **до первого `await`**, поэтому
  прямой `_model.update` оттуда дёргает `markNeedsBuild` посреди сборки
  элемента. Применять состояние только через
  `SchedulerBinding.instance.runOutsideFrame`. И `_initCompleter` завершать
  раньше всего, что может бросить: иначе `_performAsyncDispose` паркуется на
  нём навсегда, и сьюта не падает, а **висит** (волна 10);
- **ошибка внутри `yield*`-делегированного потока идёт прямо подписчику** —
  `try/catch` в генераторе её не видит, нужен явный `await for`;
- **ошибка, проброшенная из `_performAsyncDispose()`, до
  `tester.takeException()` не доходит**: метод вызывается на отброшенном
  future, поэтому такие тесты оборачивают в `runZonedGuarded`;
- **ассерт в `didUpdateWidget` рвёт кадр**: обновление бросается на середине и
  следом прилетает вторичный ассерт фреймворка (`'_dependents.isEmpty'`).
  Ставить такие проверки в `build`, где их ловит build error boundary;
- **`close()` снимает скриншот поддерева**, поэтому фикстура для теста с
  `close()` должна иметь ненулевой размер, а не `SizedBox.shrink()`;
- **таймер, заведённый на разборе, роняет чужие виджет-тесты.** Лимит через
  `Future.timeout` заводит таймер зоны сразу, а под тестовым биндингом отмена
  подписки не завершается вовсе — таймер оставался непогашенным к концу любого
  теста с живым скоупом (`A Timer is still pending`, 17 падений). Лимиты на
  такие ожидания брать из корневой зоны: `Zone.root.createTimer` (волна 7);
- **у Flutter нет хука «зависимый начинает сборку»**: `Element.dirty` во время
  `build()` всегда `true`, идентичность `widget` не ловит `setState`, у
  `InheritedElement` подходящего хука нет. Отсюда граница сборки по кадру в
  `scope_widget_core.dart`;
- под `AutomatedTestWidgetsFlutterBinding` ошибка, лежащая в буфере потока до
  подписки (`Stream.error`), не проходит через `asyncMap` — а именно так
  подписывается `_performAsyncInit`. Ветку ошибки поднимать `async*`-потоком,
  который бросает после первого события; `Stream.error` — только внутри
  `tester.runAsync`. Разбор:
  `docs/records/2026-08-12[1]-stream-error-under-test-binding-report.md`.

## Известные проблемы

### Из ревью

**Открыто 43 находки повторного ревью** (`2026-08-16[12]-project-review.md`),
из них шесть P1 — они и блокируют публикацию:

1. ~~Первый пример README не компилируется.~~ **Закрыто**, волна 10. Три файла
   исправлены; врезка `example/README.md` теперь совпадает с
   `example/minimal/lib/main.dart` побайтно. Дыра в гейте закрыта: оба цикла
   `docs/ru/check.sh` и `stamp.sh` перечисляют `example/README.md` отдельно
   (шаблон `example/*/README.md` его не покрывает), и заведено недостающее
   зеркало `docs/ru/example/README.md` — переводов стало 16.
2. ~~`AsyncScope` и `AsyncDataScope` не пробрасывают девять унаследованных
   параметров.~~ **Закрыто**, волна 10. Оба конструктора принимают все девять,
   в том же порядке, что и `AsyncControllerScope`; обещание `doc/debug.md:177`
   стало правдой. Новый тест `test/async_scope_parameters_test.dart` проверяет
   обе замыкательные формы **со стороны конструктора** — до него вся сьюта
   ходила к этим настройкам через рукописные подклассы, поэтому дыра и была
   невидима.
3. ~~Синхронный провал инициализации не даёт ветку ошибки.~~ **Закрыто**,
   волна 10. Модель переводится в `AsyncScopeError`, ошибка по-прежнему
   репортится. Две ловушки, на которые я наступил и которые стоит помнить:
   этот `catch` выполняется **до первого `await`**, то есть внутри самого
   `performRebuild`, — прямой `_model.update` дёргает `markNeedsBuild`
   посреди сборки, поэтому применять состояние надо через
   `SchedulerBinding.instance.runOutsideFrame`; и `_initCompleter` надо
   завершать **до** всего, что может бросить, иначе разбор навсегда паркуется
   на нём (в первой редакции правки сьюта не падала, а висела).
4. ~~Падающий диспозер в `concurrent`-группе обрывает разбор соседей.~~
   **Закрыто**, волна 10. Каждая ветвь держит свою ошибку при себе
   (`handleError` до слияния, а не вокруг обхода: ошибка, дошедшая до
   слитого потока, уходит через `yield*` прямо подписчику), слияние
   дорабатывает, первая по времени ошибка перебрасывается после. `init`
   параллельной группы намеренно оставлен как был: там первая ошибка должна
   отменять остальные, и взятое проигравшими ветвями подхватывает
   последующий разбор.
5. ~~Упавший `ScopeState.onUnmount` пропускает весь синхронный unmount
   зависимостей.~~ **Закрыто**, волна 10. Две половины разведены по своим
   `try`, первая ошибка перебрасывается после обеих — как уже было в
   `disposeAsync`. По правилу «ищи в соседних реализациях» проверены все
   восемь `onUnmount` в `lib/`: форма «пользовательский код внутри `super`, а
   обязательная работа после него» встречается ровно один раз, остальные
   ставят пользовательский хук последним.
6. **Бросок из пользовательского селектора навсегда замораживает элемент
   скоупа** (`scope_widget_core.dart:357-366`): `notifyClients` вызывается до
   `super.performRebuild()`, поэтому `_dirty` не снимается, а из списка
   грязных элемент удалён — `markNeedsBuild()` дальше выходит по `if (dirty)
   return`. В release — тихая потеря реактивности поддерева. Тот же дефект по
   второму адресу: `assert(_debugCheckScopeKeyOwnership())` в
   `async_scope_core.dart:308` тоже стоит до `super.performRebuild()`.

P2 и P3 — в отчёте; половина P2 это расхождения документации с кодом
(13 расхождений на 43 проверенных утверждения). Причина структурная: врезки с
кодом в README, `doc/*.md` и дартдоке не проверяет ни один гейт — `dart doc`
не анализирует содержимое ```dart-блоков, а `docs/ru/check.sh` сверяет
blob-хеши переводов, но не соответствие врезки исходнику.

### Открыто, вне списка ревью

- **Из четырёх ожиданий разбора ограничены три; четвёртое — нет.** Не
  ограничен `await _initCompleter.future` (шаг «инициализацию дожидаются, если
  отменить не удалось»). Это не дефект, а зафиксированная граница: завершают
  этот completer сам разбор сразу после отмены и колбэки подписки, то есть код
  пакета, а не пользователя, и сценария зависания для него не нашлось. Волна 8.
- **`AsyncControllerScopeElementBase` читает приватное `_initSucceeded`**
  соседнего слоя (`AsyncScopeElementBase`). Внутри одной библиотеки законно и
  сделано намеренно: это тот же признак, по которому `_performAsyncDispose`
  решает, звать ли `disposeAsync`, и разъехаться они не могут. Связь неявная,
  поэтому закрыта комментарием в коде и мутацией в тестах.
- **`previous.pop()` обходит внешний `PopScope(canPop: false)`**, поставленный
  приложением поверх узла: системный back снимает защищённый маршрут. Поведение
  унаследованное — одинаковое до и после `45aa808`. Прямая замена на
  `previous.maybePop()` не годится: собственный `PopScope` узла зарегистрирован
  на том же маршруте и вернёт событие обратно, нужна однократная пометка
  «решение принято». Там же частный случай: единственная страница узла,
  защищённая своим `PopScope(canPop: false)`, системным back не удерживается.
  Разбор — находка 3 в
  `docs/records/2026-08-15[9]-navigation-system-back-audit-report.md`.
- **Зависимый, перестроенный дважды за один кадр, сохраняет оба набора
  селекторов** — граница сборки взята по кадру, и этот случай она не различает.
  Цена — одна лишняя сборка. Записано в `doc/b_scope_widget.md`.
- `dart format` расходится на 6 файлах `example/scopo_demo` (`box.dart`,
  `code_block.dart`, `consumer.dart`, `expansion.dart`, `titled_box.dart`,
  `scope_notifier_example1_special.dart`): их писал более старый форматтер, а
  переформатирование конфликтует с `require_trailing_commas`. В гейт §6
  форматирование примеров не входит, поэтому проверки зелёные.

### Тулчейн и окружение

- **Пол держит `logger_builder`, а не код scopo.** Сам пакет прекрасно жил бы
  на Flutter 3.27, но `logger_builder 0.5.0` требует `meta ^1.16.0` (при этом
  сам объявляет `sdk: ^3.2.0`), а `flutter_test` из 3.27 пинит `meta 1.15.0`.
  `logger_builder` — пакет владельца (`~/development/my/logger_builder`):
  ослабить там `meta` до `^1.15.0` и выпустить `0.5.1` — единственный способ
  вернуть scopo пол 3.27.
- **Не поднимать `sdk` до `^3.7.0`** вслед за полом Flutter: языковая версия 3.7
  переключает `dart format` на tall style и переформатирует пакет целиком. Код
  ничего новее 3.6 не использует.
- Расхождения между версиями анализатора реальны: два замечания `analyze`,
  закрытые в `b697775`, видел только тулчейн 3.29, но не 3.44.9. Отсюда правило
  работать на поле.
- Flutter 3.29.0 с Xcode 26.5 печатает warning о двух совпавших macOS
  destinations. Косметика старого Flutter tools, release всё равно universal;
  upstream исправил в 3.35.0. Не подавлять. Разбор:
  `docs/records/2026-08-14[8]-xcode-destination-warning-report.md`.
- `flutter run -d macos --release` не завершается следом за закрытым
  приложением — открытый баг Flutter tools `flutter/flutter#58459`, не утечка
  scopo. Для проверки release собирать `fvm flutter build macos --release` и
  открывать `.app` напрямую. Разбор:
  `docs/records/2026-08-14[7]-release-run-hang-report.md`.

### В рабочем дереве

- untracked-каталог `.claude/` — чужой, не трогать и не добавлять в индекс;
- в `git stash` лежит чужой давний стеш `90ccbbc update README` — не трогать;
- корневой `pubspec.lock` в `.gitignore`; локи трёх примеров закоммичены и
  пересозданы закреплённым тулчейном.

## Пробелы в покрытии

Раздел переписан по факту после повторного ревью: прежняя формулировка
«крупных пробелов не осталось, покрыты все слои» была неверна.

Покрыто сильно: освобождение `scopeKey`
(`test/async_scope_coordinator_test.dart`), порядок разбора на обоих путях
(`test/scope_lifecycle_order_test.dart`), `close()` во всех четырёх состояниях
и `ScreenshotReplacer` прямыми виджет-тестами (`test/lite_scope_test.dart`),
дерево из десяти зависимостей с четырнадцатью комбинациями падений
(`test/scope_auto_dependencies_test.dart`), `throwWhenDisposed` прямо, с
проверкой текста (`test/listenable_utils_test.dart`), пять путей разбора
контроллера (`test/async_controller_scope_test.dart`).

**Пробелы, найденные ревью** (по убыванию риска):

- **дерево зависимостей, отменённое на полпути через виджет** — все
  виджет-тесты `Scope` используют синхронные инициализаторы, так что дерево
  всегда успевает достроиться. Помощник `handleInit(cancel:)` в тестах
  существует, но **ни один живой тест его не передаёт**;
- **падающие хуки контроллера** — `cleanup_after_user_error_test.dart` имеет
  группы для `AsyncScope`, `AsyncDataScope` и `Scope`, и не имеет для
  `AsyncControllerScope`;
- **поверхность параметров публичных виджетов** — ни один тест не собирает
  `AsyncScope`/`AsyncDataScope` с пер-скоупным таймаутом; такой тест сегодня
  не скомпилировался бы (P1 №2);
- **публичные фасады без единого теста**: `LiteScope`, `LiteScopeState`,
  `ScopeWidgetBase`, `ScopeStateNotifier`, `ScopeStateWithErrorNotifier`;
- **затенение скоупа того же типа вложенным** — базовый инвариант DI не
  проверен ни разу;
- **ветка «брошенное ожидание завершилось ошибкой»** в `_awaitBounded`: все
  тесты истечений держат `Completer`, который никогда не завершается;
- **фильтрация по уровню логирования** — дефолт `off`, издатель `print`, порог
  живёт во внешнем `logger_builder` и ничем не закреплён;
- **связка `isRoot` + `onPop`** (пустой внутренний стек, наоборот, покрыт).

Тесты-пустышки: `test/async_scope_state_test.dart` целиком (оба теста считают
скобки и пройдут при `toString() => ''`), тавтологический ассерт в
`test/scope_logger_test.dart:32`.

Что осознанно не покрыто и покрыто быть не может: **закрытость
`State.dispose` от переопределения** — гарантия анализаторная
(`@nonVirtual` → warning); то же у закрытых обёрток `ScopeController`.

## Что дальше

1. **Закрыть шесть P1** повторного ревью — список выше, план с оценками в
   конце `2026-08-16[12]-project-review.md`. Четыре из шести — правки на
   несколько строк; P1 №4 и №6 требуют теста и аккуратности.
2. **Разобрать P2 одной волной.** Половина из них — расхождения документации
   с кодом; после публикации они превратятся в «поведение, на которое кто-то
   полагается».
3. P3 (20 находок) — одной волной после P1/P2, это качество публичной
   поверхности перед 1.0.
4. Затем финальное ревью и публикация 0.10.0 — по явной просьбе владельца, не
   раньше.

Каждая волна закрывается по правилу, выведенному ревью: **найденное
исправление ищется в соседних реализациях** (последовательная/параллельная
группа, синхронная/асинхронная половина разбора, три семейства с одинаковыми
полями), и результат поиска пишется в отчёт волны.

`docs/backlog.md` **пуст**: все четыре записи владельца закрыты — `de83d9c`
(`progress` в билдерах `AsyncScope`), `145775c` (порядок параметров типа у
`AsyncDataScope.select`), `43c18ef` (почему значение отдают билдеру) и
`ff2df2f` с `e2005d7` (семейство `AsyncControllerScope` и демо к нему).

## Как здесь работают

Полный регламент — в `AGENTS.md`. Коротко: тест до исправления и проверка
нагруженности откатом (мутацией); `docs/handoff.md` обновляется по ходу, а не в
конце; выполненное удаляется из `docs/backlog.md`; мерж прямо в `main`; чужие
правки в коммит не попадают; правка оригинала в `doc/` не заканчивается без
правки зеркала в `docs/ru/` и `sh docs/ru/stamp.sh`; перед мержем прогоняются
восемь команд из раздела 6 `AGENTS.md` (через `fvm`, тулчейн 3.29.0).
