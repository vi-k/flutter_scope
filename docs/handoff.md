# Состояние проекта

> Обновлено: 2026-08-13. Файл поддерживается агентом и переписывается по ходу
> работы — это снимок «здесь и сейчас», а не история (история — в
> `docs/records/`) и не список пожеланий (он в `docs/backlog.md`).

## С чего начать после рестарта

1. Прочитай этот файл целиком, потом `docs/backlog.md`.
2. Сверься: `git log --oneline -5`, `git status`.
3. Работа в процессе — см. «Где мы сейчас»; продолжай со следующей страницы
   документации либо бери шаг из «Что дальше».

## Где мы сейчас

Ветка `main` (`805efa6`), рабочее дерево чистое, других веток и worktree нет.
Версия **0.10.0 подготовлена, но не опубликована** на pub.dev.

В работе — **документация**: пишем 8 заглушек `doc/*.md` по образцу `h_scope`
(полный разбор), по одной странице за раз, каждая с русским зеркалом в том же
коммите. Образец (`a_base`) владелец утвердил.

Написаны 4 из 8: `a_base` (`17f5297`), `b_scope_widget` (`db579f5`),
`c_scope_model` (`596f009`), `d_scope_notifier` (`805efa6`). Осталось:
**`e_async_scope`** (следующая), `f_async_data_scope`, `g_lite_scope`,
`j_utils`.

Приём, который себя оправдал: примеры со страницы собираются во временный файл
`test/tmp_doc_check.dart`, прогоняются через `analyze` и файл удаляется. Первый
вариант `CartScope` так не скомпилировался — объявлял поле `child`, которое уже
есть у `InheritedWidget`.

**У каждой публичной страницы есть русское зеркало в `docs/ru/`** (правило —
`AGENTS.md` §7). Новая страница `doc/*.md` без зеркала роняет проверку
`sh docs/ru/check.sh`, она же ловит правку оригинала без правки перевода.
Обновил перевод — прогони `sh docs/ru/stamp.sh`.

Последние эпизоды:

* **Русские зеркала** (`1952a2e`…`98010f8`) — `docs/ru/` с переводами
  `README.md` и всех девяти `doc/*.md` (семь заглушек зеркалированы как
  заглушки). Актуальность проверяемая: в шапке перевода записан blob-хеш
  оригинала, `docs/ru/check.sh` сверяет его с `git hash-object` и стал седьмой
  командой в `AGENTS.md` §6. `docs/ru/stamp.sh` переносит хеши после того, как
  перевод действительно обновлён, и честно предупреждает о себе: текста он не
  читает, свежий перевод от устаревшего не отличит.
* **`doc/a_base.md`** (`17f5297`) — первая из восьми. Два утверждения страницы
  про подписки не были закреплены тестами, поэтому в `test/base_test.dart`
  добавлена группа `what a dependent subscribes to`: изменение невыбранного
  значения не будит зависимого, а `of(listen: true)` не сужается селектором в
  том же `build`. Тесты нагруженные (мутация в `_updateDependencies` ловится).
  Тест-сьют: 144 → 146.
* **Категории dartdoc** (`66dfc85`, `dedb888`) — пункт бэклога закрыт.
  30 публичных типов не входили ни в одну категорию: 15 из слоёв скоупов
  (шесть названы в бэклоге, остальные — state-классы) получили категорию
  своего слоя, 15 хелперов из `lib/src/utils/**` — новую категорию `utils` со
  страницей `doc/j_utils.md` (заглушка, теперь их восемь). Без категории
  остались только `AsyncScopeModel` и `NodeNavigatorState` — они `@nodoc`.
  `CompareUtils` пришлось лечить как `ScopeConfig`: dartdoc-комментарий
  сместил привязку `avoid_classes_with_only_static_members` выше строки
  `// ignore:`, подавление стало файловым.
* **Тулчейн и пол версий.** Работаем на **Flutter 3.29.0 через fvm** — это
  ровно заявленный в `pubspec.yaml` пол, поэтому проверки из `AGENTS.md`
  §6 и есть его проверка. По дороге сюда: старый пин 3.29.2 сняли, погоняли на
  глобальном 3.44.9, вернули проверку пола отдельным шагом — и она сразу нашла
  дефект упаковки. Пол `>=3.27.0` **никогда не резолвился**: `logger_builder`
  требует `meta ^1.16.0`, а `flutter_test` из 3.27 пинит `meta 1.15.0`. Пол
  поднят до 3.29.0 (`fb10f94`), `meta` убран из `dependencies` — в `lib/` его
  никто не импортирует, он нужен одному тестовому хелперу. Локи примеров
  пересозданы 3.29.0, кеш fvm подчищен (3.27.0 и 3.29.2 удалены).
* **Два замечания `analyze`** (`b697775`) — закрыты, и заодно объяснено, откуда
  взялись: прошлые сессии гоняли `flutter` с `PATH` (3.44.9), а не закреплённый
  3.29.2. Подавление `avoid_classes_with_only_static_members` в
  `scope_config.dart` не работало ни там, ни там по-настоящему — тулчейны
  привязывают диагностику к разным строкам, поэтому теперь оно файловое;
  колбэки в `screenshot_replacer.dart` отдают future в `unawaited`. Оба
  тулчейна показывают «No issues found».
* **`ScopeAutoDependenciesProgress`** (`9cbd3d5`, пункт бэклога закрыт) — поле
  `name` держало весь путь от корня дерева зависимостей и переименовано в
  `path`; `name` осталось и означает теперь последний сегмент пути, то есть имя,
  с которым зависимость объявлена. Правка breaking и для `name` молчаливая
  (компилируется, но показывает другое) — сказано в `CHANGELOG.md` (0.10.0),
  сделано до публикации 0.10.0 намеренно.
* **Именование записей** (`30fe519`) — файлы в `docs/records/` кончаются жанром
  (`design`, `plan`, `report`, `review`, `log`), правило в `AGENTS.md` §4.

Далее — эпизоды прошлой сессии (подробности в
`docs/records/2026-08-13[1]-debt-and-coverage-report.md`):

* **Технический долг и `TODO(nashol)`** — разобран. Попутно найден и исправлен
  реальный дефект (`d801d83`): провал отмены инициализации бросал утилизацию,
  не сняв скоуп с учёта у родителя и не освободив `scopeKey`. Запись в
  `CHANGELOG.md` (0.10.0) добавлена.
* **Фаза покрытия** — закрыты оба пункта тестового бэклога, все слои без
  тестов, освобождение `scopeKey` и утилиты. Тест-сьют: 92 → 143.
* **Регламент агентов** (2026-08-12) — `AGENTS.md` как точка входа, этот файл,
  `docs/backlog.md`, `docs/architecture.md`, история в `docs/records/`.

Более ранняя история — в `docs/records/`, начиная с
`2026-07-30[1]-audit-fixes-plan.md`.

## Проверки

Прогон 2026-08-13 на рабочем тулчейне (Flutter 3.29.0 / Dart 3.7.0 через fvm,
он же заявленный пол): `fvm flutter test` — **144 теста, все зелёные**;
`fvm flutter analyze` — чисто в корне и в обоих примерах; `dart format` без
изменений; `dart doc` 0/0; `dart pub publish --dry-run` — **0 предупреждений**.
После `17f5297` тестов 146.

Тот же набор до перехода прошёл и на глобальном 3.44.9 — пакет держится на
обеих версиях.

## Известные проблемы

- **Пол держит `logger_builder`, а не код scopo.** Сам пакет прекрасно жил бы
  на Flutter 3.27, но `logger_builder 0.5.0` требует `meta ^1.16.0` (при этом
  сам объявляет `sdk: ^3.2.0` — то есть обещает больше, чем может), а
  `flutter_test` из 3.27 пинит `meta 1.15.0`. `logger_builder` — пакет
  владельца (`~/development/my/logger_builder`): ослабить там `meta` до
  `^1.15.0` и выпустить `0.5.1` — единственный способ вернуть scopo пол 3.27.
- Расхождения между версиями анализатора реальны: два замечания `analyze`,
  закрытые в `b697775`, видел только тулчейн ветки 3.29, но не 3.44.9. Отсюда
  и правило работать на поле: что чисто на 3.44.9, может быть не чисто у
  пользователя на 3.29.
- Поднимать `sdk` до `^3.7.0` вслед за полом Flutter **не надо**: языковая
  версия 3.7 переключает `dart format` на tall style и переформатирует пакет
  целиком. Код ничего новее 3.6 не использует.
- Скоуп, чей `initAsync()`-генератор приостановлен на future, который никогда
  не завершится, невозможно отменить: `subscription.cancel()` не завершается, и
  утилизация не идёт дальше. Родитель выходит по своему
  `waitForChildrenTimeout`, но `scopeKey` такой скоуп не освобождает никогда.
  Найдено попутно при работе над отменой инициализации; отдельного теста нет.
- `dart format` расходится на 6 файлах `example/scopo_demo` (`box.dart`,
  `code_block.dart`, `consumer.dart`, `expansion.dart`, `titled_box.dart`,
  `scope_notifier_example1_special.dart`): их писал более старый форматтер, а
  переформатирование конфликтует с `require_trailing_commas`. В гейт из
  `AGENTS.md` §6 форматирование примеров не входит, поэтому проверки зелёные;
  чинить — отдельная задача.
- Тестовое ограничение, не дефект: под `AutomatedTestWidgetsFlutterBinding`
  ошибка, лежащая в буфере потока до подписки (`Stream.error`), не проходит
  через `asyncMap` — а именно так подписывается `_performAsyncInit`. Ветку
  ошибки в тестах поднимать `async*`-потоком, который бросает после первого
  события; `Stream.error` — только внутри `tester.runAsync`. Разбор:
  `docs/records/2026-08-12[1]-stream-error-under-test-binding-report.md`.
- Мелкая несогласованность публичного API, найдена при написании
  `doc/d_scope_notifier.md`: у `ScopeNotifier.value` нет параметра `tag`, хотя
  он есть у `ScopeNotifier`, `ScopeModel.value` и `ScopeNotifierBase.value`.
  Не чинил — правка breaking-безопасная, но выходит за объём задачи.
- В `git stash` лежит чужой давний стеш `90ccbbc update README` — не трогать.

## Пробелы в покрытии

Крупных пробелов не осталось. Покрыты все слои, освобождение `scopeKey`
(`test/async_scope_coordinator_test.dart`), обычные пути `waitingBuilder`,
`tag` и `pauseAfterInitialization` (`test/async_scope_paths_test.dart`),
утилиты (`test/listenable_utils_test.dart`, `test/progress_iterator_test.dart`,
`test/navigation_node_test.dart`).

Что осознанно не покрыто:

- `NavigationNode.onPop` и `isRoot`: перехват системного «назад» и проброс попа
  наружу через `_HookEntry` — нужен сценарий с системной кнопкой, тестов нет;
- `run_stream_guarded` и `throwWhenDisposed` проверяются только косвенно, через
  дерево зависимостей и подписки;
- `ScreenshotReplacer` покрыт со стороны `LiteScope.close()`, отдельных тестов
  на сам виджет нет.

## Что дальше

Приоритет не выбран владельцем. Кандидаты, от наиболее готового к работе:

1. **Документация** — 7 оставшихся заглушек `doc/*.md` (следующая
   `b_scope_widget`), описание примеров, скриншоты для pub.dev (секции
   `screenshots:` в `pubspec.yaml` нет).
2. **`example` для `ScopeWidgetCore`** — единственный оставшийся пункт бэклога
   про код.
3. **Публикация 0.10.0** — все проверки чистые, ничего технического не
   мешает; решение о выпуске — за владельцем.

Breaking-правки публичного API дешевле делать **до** публикации 0.10.0: после
неё каждая требует отдельного выпуска. В бэклоге таких не осталось —
`example` для `ScopeWidgetCore` API не меняет.

## Как здесь работают

Полный регламент — в `AGENTS.md`. Коротко: тест до исправления и проверка
нагруженности откатом; `docs/handoff.md` обновляется по ходу, а не в конце;
выполненное удаляется из `docs/backlog.md`; мерж прямо в `main`; чужие правки
в коммит не попадают; перед мержем прогоняются семь команд из раздела 6
`AGENTS.md` (через `fvm`, тулчейн 3.29.0).
