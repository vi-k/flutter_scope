# Состояние проекта

> Обновлено: 2026-08-15. Файл поддерживается агентом и переписывается по ходу
> работы — это снимок «здесь и сейчас», а не история (история — в
> `docs/records/`) и не список пожеланий (он в `docs/backlog.md`).

## С чего начать после рестарта

1. Прочитай этот файл целиком, потом `docs/backlog.md`.
2. Сверься: `git log --oneline -5`, `git status`.
3. Документация учебного кода всех девяти демонстраций `example/scopo_demo`
   завершена: исполняемый Dart-код не менялся, после исправлений финального
   ревью в `9023f0c` полный гейт повторно пройден. Дизайн и выполненный план —
   в
   `docs/records/2026-08-14[5]-example-code-documentation-design.md` и
   `docs/records/2026-08-14[6]-example-code-documentation-plan.md`.

## Где мы сейчас

Ветка `main`. Все девять демонстраций `example/scopo_demo` получили
содержательные английские комментарии; исполняемый Dart-код не менялся.
Полный гейт повторно пройден после финального ревью, включая
`dart pub publish --dry-run` с 0 warnings. Служебный UI, fake-сервисы и
`example/minimal` в охват не входят.
Владелец решил не добавлять скриншоты для pub.dev: статичные кадры технического
демо не объясняют ценность инфраструктурного пакета и не дают pub points.
Полное независимое ревью перед публикацией завершено: подтверждено 18 находок
(0 P0, 3 P1, 12 P2, 3 P3), код не исправлялся. Три блокера выпуска: слишком
ранний вызов документированного `create(context)`, обход вложенного
`Navigator` системным back и утечка частично созданной зависимости после
ошибки/отмены. Полный отчёт —
`docs/records/2026-08-14[10]-project-review.md`, выполненный план —
`docs/records/2026-08-14[9]-project-review-plan.md`. Три изолированных
Codex-ревьюера участвовали; Claude CLI 2.1.232 установлен, но не авторизован и
до чтения кода не дошёл. Десять временных регрессионных проб подтвердили
выбранные дефекты и удалены. Новые записи владельца в `docs/backlog.md` не
относятся к ревью и остаются в работе.
Других веток и worktree нет. Версия **0.10.0 подготовлена, но не
опубликована** на pub.dev; до исправления P1 публикация не рекомендована.
Владелец решил исправлять все находки по порядку и публиковать пакет только
после закрытия всего списка. Первая волна — P1 №1, слишком ранний
`create(context)`. Дизайн переноса общего `init()` из конструктора элемента в
первый `performRebuild` после mount согласован и записан в
`docs/records/2026-08-15[1]-scope-init-after-mount-design.md`; код ещё не
менялся, документ ожидает финального подтверждения владельца перед планом.

Диагностика «зависшего» release-приложения завершена: само `scopo_demo`
штатно завершается после закрытия окна, а резидентным остаётся породивший его
`flutter run`. Это ограничение Flutter CLI, а не scopo; код не менялся.
Подтверждение и рабочий обход через `flutter build macos --release` с прямым
запуском `.app` — в
`docs/records/2026-08-14[7]-release-run-hang-report.md` (`84ea052`).

Диагностика warning `xcodebuild` о двух совпавших macOS destinations
завершена: Flutter 3.29.0 передаёт слишком широкий `platform=macOS`, и Xcode
26.5 выбирает нативный `arm64` destination. На продукт это не влияет:
release-сборка остаётся universal (`arm64` + `x86_64`) во всех исполняемых
бинарниках. Исправление уже есть в Flutter начиная с 3.35.0; на заявленном
полу 3.29.0 warning ожидаем и не маскируем. Разбор — в
`docs/records/2026-08-14[8]-xcode-destination-warning-report.md` (`b9b0694`).

Во вкладке `ScopeWidget` теперь рядом показаны `ScopeWidgetBase` и
`ScopeWidgetCore`: второй пример хранит счётчик в собственном элементе,
вызывает команду через узкий контекст и перестраивает зависимый текст через
селектор. Реализация и widget-тест `0 → 1` — в `a8a8472`.

Документация examples в исходном коде завершена для всех девяти демонстраций;
выполненный пункт удалён из `docs/backlog.md` в коммите этой работы.

Десять тематических страниц `doc/*.md`, README обоих примеров и dartdoc у всех
281 публичного члена готовы. Скриншоты для pub.dev решено не добавлять. У
каждого публичного документа есть русское зеркало в `docs/ru/`, и
`docs/ru/check.sh` покрывает в том числе `example/*/README.md`.

**Включён линт `public_member_api_docs`.** Новый публичный член без dartdoc
роняет `flutter analyze`. Это то, что удерживает результат: раньше линт был
закомментирован, и за ним стоял 281 недокументированный член.

Предыдущая работа — пояснение колбэков `ScopeModel`: дизайн и выполненный план в
`docs/records/2026-08-14[1]-scope-model-callbacks-design.md` и
`docs/records/2026-08-14[2]-scope-model-callbacks-plan.md`.

Подробности предыдущей сессии (2026-08-13) — в
`docs/records/2026-08-13[2]-docs-and-toolchain-report.md`: тулчейн и пол
версий, восемь тематических страниц, зеркала, dartdoc у всего публичного API.
Сессия перед ней — `2026-08-13[1]-debt-and-coverage-report.md`.

Более ранняя история — в `docs/records/`, начиная с
`2026-07-30[1]-audit-fixes-plan.md`.

## Проверки

Полный гейт повторно пройден после оформления итогового отчёта на рабочем
тулчейне (Flutter 3.29.0 / Dart 3.7.0 через fvm, он же заявленный пол).

Дополнительный widget-тест нового example тоже прошёл: 1/1.

| проверка | результат |
| --- | --- |
| `fvm flutter test` | **146 тестов, все зелёные** |
| `fvm flutter analyze` (корень) | `No issues found!` |
| `analyze` в обоих `example/*` | `No issues found!` в каждом |
| `fvm dart format --set-exit-if-changed lib test` | 76 файлов, 0 changed |
| `fvm dart doc --dry-run` | 0 warnings, 0 errors |
| `fvm dart pub publish --dry-run` | 0 warnings |
| `sh docs/ru/check.sh` | переводы актуальны: 13 |

Тот же набор до перехода на 3.29.0 прошёл и на глобальном 3.44.9 — пакет
держится на обеих версиях.

## Известные проблемы

- Полное ревью выявило **3 P1, 12 P2 и 3 P3**; код пока не исправлен.
  Выпуск блокируют `create(context)` до mount, системный back мимо вложенного
  `Navigator` и пропуск disposer у частично созданной зависимости. Остальные
  находки по очистке, владению, селекторам, навигации и документации описаны в
  `docs/records/2026-08-14[10]-project-review.md`.

- **Пол держит `logger_builder`, а не код scopo.** Сам пакет прекрасно жил бы
  на Flutter 3.27, но `logger_builder 0.5.0` требует `meta ^1.16.0` (при этом
  сам объявляет `sdk: ^3.2.0` — то есть обещает больше, чем может), а
  `flutter_test` из 3.27 пинит `meta 1.15.0`. `logger_builder` — пакет
  владельца (`~/development/my/logger_builder`): ослабить там `meta` до
  `^1.15.0` и выпустить `0.5.1` — единственный способ вернуть scopo пол 3.27.
- Расхождения между версиями анализатора реальны: два замечания `analyze`,
  закрытые в `b697775`, видел только тулчейн ветки 3.29, но не 3.44.9. Отсюда
  и правило работать на поле: что чисто на 3.44.9, может быть не чисто у
  пользователя на 3.29.
- Поднимать `sdk` до `^3.7.0` вслед за полом Flutter **не надо**: языковая
  версия 3.7 переключает `dart format` на tall style и переформатирует пакет
  целиком. Код ничего новее 3.6 не использует.
- Flutter 3.29.0 с Xcode 26.5 печатает warning о выборе первого из двух
  совпавших macOS destinations (`arm64` и `x86_64`). Это косметика старого
  Flutter tools: `build_macos.dart` передаёт `-destination platform=macOS`,
  Xcode выбирает нативный `arm64`, но release всё равно собирается universal
  при `ARCHS = arm64 x86_64` и `ONLY_ACTIVE_ARCH = NO`. Upstream исправил
  команду в `flutter/flutter#165996`; исправление входит в Flutter 3.35.0 и
  новее. На поле 3.29.0 предупреждение не подавлять и проект не менять.
  Разбор:
  `docs/records/2026-08-14[8]-xcode-destination-warning-report.md`.
- `flutter run -d macos --release` не завершается следом за закрытым
  приложением: в release-режиме нет VM Service, а desktop-ветка Flutter CLI
  не завершает resident runner по `exitCode` дочернего процесса. Поведение
  подтверждено на закреплённом Flutter 3.29.0; тот же путь кода остаётся в
  локальных 3.44.4 и stable. Это не утечка scopo: процесс `scopo_demo`
  исчезает. Это известный открытый баг Flutter tools:
  `flutter/flutter#58459` (`P3`, `triaged-tool`, `tool-still-valid`, все три
  desktop-платформы); дубликат `#135019` закрыт именно как дубликат, а не как
  исправленный. Для проверки release собирать через
  `fvm flutter build macos --release` и открывать `.app` напрямую. Разбор:
  `docs/records/2026-08-14[7]-release-run-hang-report.md`.
- Скоуп, чей `initAsync()`-генератор приостановлен на future, который никогда
  не завершится, невозможно отменить: `subscription.cancel()` не завершается, и
  утилизация не идёт дальше. Родитель выходит по своему
  `waitForChildrenTimeout`, но `scopeKey` такой скоуп не освобождает никогда.
  Найдено попутно при работе над отменой инициализации; отдельного теста нет.
- `dart format` расходится на 6 файлах `example/scopo_demo` (`box.dart`,
  `code_block.dart`, `consumer.dart`, `expansion.dart`, `titled_box.dart`,
  `scope_notifier_example1_special.dart`): их писал более старый форматтер, а
  переформатирование конфликтует с `require_trailing_commas`. В гейт из
  `AGENTS.md` §6 форматирование примеров не входит, поэтому проверки зелёные;
  чинить — отдельная задача.
- Тестовое ограничение, не дефект: под `AutomatedTestWidgetsFlutterBinding`
  ошибка, лежащая в буфере потока до подписки (`Stream.error`), не проходит
  через `asyncMap` — а именно так подписывается `_performAsyncInit`. Ветку
  ошибки в тестах поднимать `async*`-потоком, который бросает после первого
  события; `Stream.error` — только внутри `tester.runAsync`. Разбор:
  `docs/records/2026-08-12[1]-stream-error-under-test-binding-report.md`.
- Мелкая несогласованность публичного API, найдена при написании
  `doc/d_scope_notifier.md`: у `ScopeNotifier.value` нет параметра `tag`, хотя
  он есть у `ScopeNotifier`, `ScopeModel.value` и `ScopeNotifierBase.value`.
  Не чинил — правка breaking-безопасная, но выходит за объём задачи.
- В `git stash` лежит чужой давний стеш `90ccbbc update README` — не трогать.

## Пробелы в покрытии

Крупных пробелов не осталось. Покрыты все слои, освобождение `scopeKey`
(`test/async_scope_coordinator_test.dart`), обычные пути `waitingBuilder`,
`tag` и `pauseAfterInitialization` (`test/async_scope_paths_test.dart`),
утилиты (`test/listenable_utils_test.dart`, `test/progress_iterator_test.dart`,
`test/navigation_node_test.dart`).

Что осознанно не покрыто:

- `NavigationNode.onPop` и `isRoot`: перехват системного «назад» и проброс попа
  наружу через `_HookEntry` — нужен сценарий с системной кнопкой, тестов нет;
- `run_stream_guarded` и `throwWhenDisposed` проверяются только косвенно, через
  дерево зависимостей и подписки;
- `ScreenshotReplacer` покрыт со стороны `LiteScope.close()`, отдельных тестов
  на сам виджет нет.

## Что дальше

Исправлять все 18 находок строго по порядку из
`docs/records/2026-08-14[10]-project-review.md`, каждую отдельной TDD-волной.
Сейчас согласованный дизайн P1 №1
(`docs/records/2026-08-15[1]-scope-init-after-mount-design.md`) ожидает
подтверждения перед составлением implementation plan. Пакет публиковать только
после закрытия всего списка, полного повторного ревью и явной просьбы
владельца.

Параллельно в `docs/backlog.md` появились три новые записи владельца про API
`AsyncScope`/`AsyncDataScope`; они не выполнены и не переписаны ревьюером.

## Как здесь работают

Полный регламент — в `AGENTS.md`. Коротко: тест до исправления и проверка
нагруженности откатом; `docs/handoff.md` обновляется по ходу, а не в конце;
выполненное удаляется из `docs/backlog.md`; мерж прямо в `main`; чужие правки
в коммит не попадают; перед мержем прогоняются семь команд из раздела 6
`AGENTS.md` (через `fvm`, тулчейн 3.29.0).
