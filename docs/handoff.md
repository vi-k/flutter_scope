# Состояние проекта

> Обновлено: 2026-08-22. Файл поддерживается агентом и переписывается по ходу
> работы — это снимок «здесь и сейчас», а не история (история — в
> `docs/records/`) и не список пожеланий (он в `docs/backlog.md`).

## С чего начать после рестарта

1. Прочитай этот файл целиком, потом `docs/backlog.md`.
2. Сверься: `git log --oneline -5`, `git status`.
3. **Очереди работ нет, и это главное.** Списки **всех четырёх ревью
   исчерпаны** — 18 + 43 + 44 + 52 = 157 находок, у каждой вердикт в своём
   файле ревью; закрыт и аудит имён, ещё 20. `docs/backlog.md` пуст. В дереве
   ничего не начато.

   **Главный открытый вопрос — публикация 0.10.0**, и он к владельцу. Ревью её
   больше не блокирует: последнее (`2026-08-20[1]-project-review.md`)
   требовало закрыть Critical и High, владелец решил чинить всё, и это сделано.
   Пакет не публикуют без явной просьбы (`AGENTS.md` §5). Второй и последний
   вопрос — в п. 7: ограничивать ли четвёртое ожидание разбора.

   **Если работа появится** — порядок обычный: §8 (сперва падающий тест, потом
   правка, потом проверка нагруженности откатом; закрыл — ищи то же у соседа) и
   §4 (у каждой проработанной находки ревью — вердикт абзацем в конце неё, в
   самом файле ревью). **Работа не из списка ревью начинается с записи дизайна**
   в `docs/records/` и с согласия владельца — так шла последняя,
   `2026-08-22[1]-build-phase-design.md`: решения о публичной поверхности
   принимает он, а не агент.

4. **Ветка `main`, других веток и worktree нет. Всё запушено.** Проверяй сам:
   `git log --oneline origin/main..HEAD` — должно быть пусто.
5. **Гейт §6 пройден целиком 2026-08-22 на полу 3.27.0**, включая сьюты обоих
   примеров, — числа в «Проверках». Прогнан на голове `main`; номер коммита
   сюда не пишется, он устаревает раньше, чем его прочтут. Зависших
   `flutter_tester` не осталось; про чужие всё равно прочти «Грабли» — они
   появляются, и рубить их вслепую нельзя.
6. **CI зелёная на голове** — `.github/workflows/ci.yml`, тот же гейт плюс
   сьюты примеров и `git diff --exit-code`, на чистом клоне. Смотреть прогоны:
   `gh run list`, `gh run view <id> --log-failed` (`gh` здесь авторизован как
   `vi-k`).
7. **Что открыто намеренно — три записи**, все в «Открыто, вне списка
   ревью»: неограниченный `await _initCompleter.future`, чтение приватного
   `_initSucceeded` соседнего слоя и два набора селекторов у зависимого,
   перестроенного дважды за кадр. **Это границы, а не пробелы** — у каждой
   записана причина. Одна из них — вопрос к владельцу, а не факт: ограничивать
   ли четвёртое ожидание.

   Четвёртой была «отказ `buildOn*` до наблюдателя не доходит»; **владелец
   решил заводить, и 2026-08-22 это сделано** — `ScopePhase.build`, `e7b01c7`.
   Дизайн — `docs/records/2026-08-22[1]-build-phase-design.md`.

   И два ограничения, которые закрыть нельзя по построению: пропавший
   `scheduleFrame()` виджет-тесту невидим (`pump()` рисует кадр безусловно), а
   release-ветки за ассертом не проверяются, потому что `flutter test` всегда с
   ассертами.
8. **Тулчейн — Flutter 3.27.0 через fvm**, он же заявленный пол. Всё гонять
   через `fvm`. Прогнал другим SDK — сразу `fvm flutter clean && fvm flutter
   pub get`, иначе следующий прогон падает на `ink_sparkle.frag`. Подробности —
   в «Тулчейн и окружение».
9. **Если помнишь пакет по прежним именам** — их нет с 2026-08-18. Главное:
   `initAsync`/`disposeAsync` разошлись на `initScope`/`disposeScope` (слой
   скоупа) и `initStateAsync`/`disposeStateAsync` (слой состояния);
   `buildOnInitializing` → `buildOnProgress`; `DepHelper` →
   `ScopeDependencyHandle`; `ScopeStateNotifier.equals` → `shouldNotify`
   **с обратным смыслом**. Полная таблица — в
   `docs/records/2026-08-18[2]-naming-plan.md`. Правило, по которому имена
   выбирались: голый глагол значит, что метод делает названное, `on…` — что он
   вызывается, когда это случилось.
10. **История — не здесь.** Что каким коммитом закрыто — в «Что дальше»: волны
    14–25 по спискам ревью, плюс пп. 9 и 14, работы не из ревью. Чем находка
    ошибалась и почему выбран такой путь — в «Известные проблемы» и в вердиктах
    самих ревью; спеки, планы и отчёты — в `docs/records/`.

## Где мы сейчас

Версия **0.10.0 подготовлена, но не опубликована** на pub.dev. Владелец решил
чинить всё до публикации и публиковать по явной просьбе. **Ревью было четыре:
18, 43, 44 и 52 находки — 157, и все закрыты**, у каждой вердикт в своём файле.
Пятым списком прошёл аудит имён (20 находок) — он не ревью кода, а разбор
словаря, и тоже закрыт.

**На 2026-08-22 незакрытых списков нет вообще**, и незакрытых записей владельца
тоже: работы в очереди — ноль. Всё, что дальше, начинается с решения владельца,
и первое из них — публиковать или нет.

**Последняя законченная работа — `ScopePhase.build`** (`e7b01c7`, 2026-08-22):
отказ сборки скоупа теперь доходит до наблюдателя. Это была одна из двух
записей «Открыто, вне списка ревью», помеченных как вопрос к владельцу, а не
факт; владелец решил заводить. Закрыта последняя семья пользовательских точек
без отчётности: `buildOnReady`, `buildOnProgress`, `buildOnError`,
`buildOnWaiting`, `buildOnClosing`, `ScopeWidgetBase.build` и
`ScopeModel.build`. Дизайн — `docs/records/2026-08-22[1]-build-phase-design.md`,
разбор — в «Открыто, вне списка ревью». Тестов **451**, гейт §6 пройден целиком
на этой голове, включая сьюты обоих примеров. **Правка ломающая** — новое
значение в публичном `enum`, — и потому сделана до публикации: седьмая в
наборе 0.10.0.

**Ревью закрыто раньше того** — четвёртое, целиком, десятью
волнами (16–25), 2026-08-20…22. Разбор — в «Что дальше», пп. 10–13; чем волны
кончились по существу, а не по коммитам, — там же. Тестов 410 → **449**, архив
пакета 364 → **238 КБ** (из него ушла сьюта). Три ломающих правки, сделанные
намеренно до публикации: запрет `ScopeTimeout.none` на отмене и на паузе,
запечатывание `ScreenshotReplacer` и `ListenableSelector`, отказ
`CompositeListenableSubscription.add` после `cancel` через ассерт вместо
броска. Прибавилось два публичных имени: `NavigationNode.enabled` и
`ScopeCompositeObserver`; третьим — `ScopePhase.build`, см. выше.

**Полное независимое ревью перед публикацией** —
`docs/records/2026-08-14[10]-project-review.md` (выполненный план — `[9]`).
Подтверждено 18 находок: 0 P0, 3 P1, 12 P2, 3 P3.

**Закрыты все 18.** Список ревью исчерпан. После него закрыт и бэклог
владельца — последней записью в пакет добавилось **новое семейство
`AsyncControllerScope`** (скоуп, владеющий контроллером с собственным жизненным
циклом; дизайн, план и отчёт — записи `2026-08-16[9]`–`[11]`).

**Повторное полное ревью проведено** — `docs/records/2026-08-16[12]-project-review.md`.
Подтверждено 43 находки: **0 P0, 6 P1, 17 P2, 20 P3**. Код при ревью не
менялся. **Закрыты все 43** — P1 волной 10, P2 волной 11, P3 волной 12.

**Волна 13 закрыла список непокрытого** — семь пробелов, отчёт
`2026-08-17[2]-coverage-gaps-report.md`. Тестов 264 → **293**. Пробелы прятали
три дефекта, все исправлены: `LiteScope.wrapState` не вызывался вовсе,
корневой `NavigationNode` с `onPop` всё-таки ронял маршрут снаружи, а примеры
защиты инициализации в трёх темах были написаны через `catch`, который отмена
не задевает.

**Третье (финальное) ревью проведено** —
`docs/records/2026-08-17[3]-project-review.md`. Подтверждено **44 находки:
0 P0, 7 P1, 16 P2, 21 P3**. Код при ревью не менялся, гейт был зелёный целиком.
**Закрыты двадцать четыре: все семь P1, все шестнадцать P2 и P3-2.** P3-2
шагом 0 волны 14, P1-1 и P1-2 её блоком B, P2-8 вне очереди, P1-3/P1-4/P1-7
блоком навигации, P1-5 и P1-6 блоком типов, а P2 — девятью блоками:
P2-1/P2-2, P2-3/P2-4, P2-5/P2-9, P2-7, P2-10/P2-15/P2-16, P2-6, P2-11/P2-14,
P2-12 и P2-13; впереди двадцать P3.

Ревью проверяло утверждения не рассуждением, а запуском: в копии репозитория
за пределами проекта прогнаны 19 мутаций кода и 12 адресных проб. Шесть из
семи P1 подтверждены выполнением кода. **15 мутаций из 19 сьюта не заметила** —
таблица в разделе «Testing review» отчёта.

Главный вывод третьего ревью, важный для дальнейшей работы: **шесть P1 из семи —
это тот же дефект в соседней реализации.** `unmount` при провале инициализации
решён в контроллерном семействе и не решён в `Scope`; опустошение навигатора
починили корневому `NavigationNode` и не посмотрели обычный; `this as T` закрыли
в `asStream` и оставили в `ScopeAutoDependencies`; правку дартдока про `null`
сделали для `ScopeConfig` (`CHANGELOG.md:451-453`) и не довели до слоя виджетов,
где его читают. То есть правило «закрыл находку — найди её же в соседней
реализации» верное, но применялось выборочно. Третье ревью добавляет к нему
второе: **соседей ищем не глазами, а мутацией** — сломать строку и посмотреть,
покраснеет ли сьюта.

### Волна 15: P3 блоками, начиная с инфраструктуры

Волна 14 закрыла свой список целиком (все P1 и все P2), поэтому работа по P3 —
**новая волна, 15-я**. Шла теми же блоками по родству; номера находок — из
третьего ревью. **Волна закончена: 44 находки из 44 закрыты, список третьего
ревью исчерпан.** Три находки из двадцати одной P3 при этом не подтвердились или
подтвердились иначе, чем написано в ревью, — P3-3, P3-20 и наполовину P3-16;
вердикты стоят в самом ревью.

| | что | коммиты | отчёт |
| --- | --- | --- | --- |
| ✔ инфраструктура | P3-1 (CI), инфраструктурная половина P3-21, leak-трекер в примерах | `b4aa330`, `c625776`, `e7d29f3` | — |
| ✔ навигация | P3-5, P3-6, P3-7, плюс застрявший `_forwarding` вне ревью | `6e194f2` | — |
| ✔ порядок освобождения | P3-3 (не подтвердилась), P3-8, плюс отказ `onCompleted` вне ревью | `3979229` | — |
| ✔ диагностика | P3-9 … P3-13 | `a9f0987` | — |
| ✔ дартдок и темы | P3-14, P3-15, P3-17, P3-18, P3-19, вторая половина P3-21 | `945175e` | — |
| ✔ решения | P3-4, P3-16, P3-20 | `1526cb9` | — |
| | пункт бэклога: переименования | | |

**Блок инфраструктуры.** Что стоит унести дальше:

- **корневой `analyze` в чистом клоне падает, и локальный гейт этого не
  показывает.** `flutter analyze` из корня обходит всё дерево, включая
  `example/`, а на свежем клоне их пакеты не разрешены: 60 ошибок про
  несуществующие `package:`-URI. Локально шаг проходит только потому, что
  примеры когда-то фетчили. Нашла это первая же попытка прогона CI — то есть
  гейт держался на необъявленном состоянии рабочего дерева. В CI `pub get`
  примеров стоит **до** корневого анализа;
- **три шага «analyze в примере» — почти повтор корневого.** Проба с нарочно
  разлаженным `dev_dependencies` показала: корневой прогон сообщает и про
  `example/navigation_node/pubspec.yaml`. Анализатор берёт для каждого файла
  ближайший `analysis_options.yaml`, то есть примеры и так проверяются своими
  правилами. Шаги оставлены: их перечисляет §6, и по имени шага видно, какой
  пример упал;
- **`pub publish --dry-run` считает незакоммиченную правку предупреждением, а
  предупреждение — это выход 65.** Поэтому в CI он идёт первым, до всего, что
  пишет в дерево. Пubignore-нутые файлы (`docs/backlog.md`, например) он при
  этом не считает — гейт зелёный и с правкой владельца в дереве;
- **severity в `analysis_options.yaml` не включает правило.** Три записи
  раздела `errors:` относились к правилам, которых нет: у
  `no_logic_in_create_state` правило прямо выключено ниже, у
  `prefer_relative_imports` и `unsafe_html` — не включено вовсе. Доказано самим
  репозиторием: фабрики `createState` нарушают первое правило, а `analyze`
  молчит;
- **у находки P3-21 сдвинуты координаты мёртвых записей**: `:43` — это живой
  `empty_catches` (правило включает набор `core`). Мёртвых было три, не две;
- **`.vscode/` в архив не попадал и до правки** — он исключён глобально, а не
  через `.gitignore`, и pub спрашивает git. В `.pubignore` записан всё равно: на
  другой машине глобального исключения может не быть;
- **строгий конфиг в примерах — это 64 замечания**, и 63 из них правятся
  механически. Единственное правило, выключенное с причиной, —
  `no_adjacent_strings_in_list` в `navigation_node`: уроки там проза, каждый
  абзац — одна строка, перенесённая по строчкам внутри списка, и правило не
  отличает это от забытой запятой. Три правила выключены в обоих примерах ровно
  те же, что выключает `example/scopo_demo`.

**Блок навигации.** Что стоит унести дальше:

- **обработчик ставят на цепочку, а не рядом с `onValue`.** `then(onValue,
  onError:)` отвечает за future, к которому прицеплен, и не за колбэк рядом с
  ним; `future.then(…).onError<Object>(…)` — за оба. Это была P3-5, и в `lib/`
  такая форма была ровно одна;
- **флаг «поднял — сделал — опустил» без `finally` — застрявший флаг.** У узла
  так жил `_forwarding` («отступил в сторону на длину одного чтения»), а чтение
  маршрута читает каждый guard на нём, то есть пользовательский код. Дефект в
  ревью не назван, нашёлся тестом к P3-5. Соседей в пакете нет: счётчик
  пересборок уже в `try/finally`, `_buildPassEnding` опускается пост-кадровым
  колбэком (это P3-4);
- **бросок пользовательского кода из попа тестом не закрывается.**
  `PopScope.onPopInvokedWithResult`, который роняет на `didPop: true`, бросает
  внутри `NavigatorState._flushHistoryUpdates`, и `_debugLocked` остаётся
  поднятым: при разборе дерева прилетает вторичный ассерт `'!_debugLocked'` из
  `NavigatorState.dispose`. Отказ для теста переносят на строку раньше — в
  чтение `route.popDisposition`, где тот же `onValue` и тот же пользовательский
  код, а фреймворк остаётся целым;
- **guard в тесте должен быть одноразовым.** Фреймворк читает `popDisposition`
  на каждом изменении истории навигатора, так что guard, который роняет всегда,
  репортит из мест, к находке не относящихся. Взводить из ответа `onPop` (между
  взводом и чтением узла — только микротаск), разряжать самим чтением;
- **лишний диф страниц стоит два `NavigationNotification` на перестройку.**
  Измерено счётчиком в тесте: 1 → 3 на одну пустую перестройку узла. То есть
  «`pages` — новый список на каждый build» слышно не только процессору: об этом
  узнаёт каждый слушатель выше узла.

**Блок порядка освобождения.** Обе находки этого блока про порядок двух строк,
и обе решились не так, как ожидало ревью. Что унести дальше:

- **бросок из `didUpdateWidget` не доходит до `dispose`, и это объясняет
  «оговорку» P3-3.** Элемент выше ловит отказ обновления
  (`ComponentElement.performRebuild` оборачивает `updateChild` в `try/catch`),
  ставит error-widget на место поддерева и **бросает прежний элемент, не
  деактивируя его**. Отсюда и утечка, которую видит трекер (лечится
  `unmountableTree`), и то, что никакого «второго `cancel`» не бывает: состояние
  больше никто не спрашивает, а следующая конфигурация строит свежее;
- **направление ревью бывает хуже нынешнего кода, и это проверяется пробой.**
  Подписаться во временную переменную и отменить старую после — значит оставить
  старого слушателя на listenable при упавшем селекторе. Мера: `hasListeners`
  после отказа — `true` при перевёрнутом порядке, `false` при нынешнем;
- **пользовательский колбэк, вызванный из `unawaited`-future, нужно оборачивать.**
  `ScreenshotReplacer.onCompleted` звали из пост-кадрового захвата и из
  `dispose`; бросок оттуда либо становился необработанной ошибкой зоны, либо
  выходил из `State.dispose` и рвал разбор дерева. Это дефект вне ревью, найденный
  пробой к P3-8. Остальные пользовательские хуки пакета зовутся из жизненного
  цикла скоупа или из `build`, где отказ уже обработан;
- **условия находки бывают взаимоисключающими.** У P3-8: успешный захват
  присваивает `_image` и отчитывается в том же синхронном шаге, поэтому
  «отчитывается `dispose`» и «есть картинка» вместе не встречаются. Проверять
  такое стоит до правки: порядок всё равно перевёрнут (он ничего не стоит), но
  теста на «потерянную память» не бывает.

**Блок диагностики.** Пять находок про то, что пакет говорит о себе. Что унести
дальше:

- **лживый тип обманывает не будущего читателя, а сьюту.** `ScopeLog.message`
  был `String?` и никогда не отдавал `null`; после смены типа анализатор показал
  две мёртвые ветки в собственных тестах пакета (`?? ''` и `'${...}'`). Правило:
  сменил тип на честный — посмотри, что скажет анализатор про своих же
  читателей;
- **имя элемента и имя виджета — разные имена, и `tag` живёт во втором.**
  У элемента `toStringShort()` — это `$E(#hash)`, у виджета — `$W(tag)`. Научить
  элемент отвечать именем виджета нельзя: `widget` у размонтированного элемента
  бросает (та же грабля, что в дефекте контроллера волны 14). Поэтому у
  `AsyncScopeParent` появился шов `reportName`, а не приведение типа;
- **ленивое сообщение — это пользовательский код, вызванный когда угодно.**
  `_log.d(() => '… [$scopeKey]')` резолвится в момент форматирования, то есть
  геттер пользователя может быть вызван из разбора, который уже начался. Тест
  ловит это счётчиком чтений у конкретного элемента (два против одного), а не
  сравнением строк;
- **два поля, присваиваемые по очереди, — это два факта, а не один.**
  `_acquiredScopeKey` пишется до поиска координатора, а поиск — единственный шаг,
  который может упасть с уже прочитанным ключом. Диагностика, считавшая их одним
  фактом, называла очередь, которой нет (P3-12);
- **где инвариант держится одним условием, ставят ассерт, а не молчаливую
  охрану.** `if (!isCompleted)` спрятал бы день, когда условие перестанет
  работать; ассерт называет его. Так закрыта P3-13.

**Блок дартдока и тем.** Что унести дальше:

- **половина находки может быть закрыта раньше, чем до неё дошли.** У P3-14
  четыре пункта из шести закрыл блок типов волны 14 (P1-6) — ревью писалось до
  него. Прежде чем чинить находку про документацию, стоит посмотреть, что уже
  изменилось;
- **сосед у находки про документацию такой же, как у находки про код.** Те же
  пробелы дартдока, что у `AsyncDataScope`, были у контроллерного семейства, а
  одной фразы не было ни у одного из трёх — включая эталонный `AsyncScope`;
- **счётчик в тексте проверяется пересчётом, а не доверием к ревью.** Ревью
  говорило про четыре разошедшихся счётчика в темах; пересчитаны все пятнадцать,
  разошлись два (`base.md` — «Four consequences» при пяти абзацах,
  `scope_notifier.md` — «Four types» при шести типах, причём шестого не было и в
  таблице). Остальные тринадцать верны;
- **утверждение про «всегда одна ошибка» проверяется тестом, а не чтением.**
  P3-19: `concurrent`-группа из двух детей, падающих в один момент фейковых
  часов, показывает ровно одну ошибку в состоянии. Отсюда же решение оставить
  `join(', ')`: диагностика не должна быть тем, что бросает, а `single` бросил
  бы;
- **мёртвая ветка от лживого типа сначала находится в собственных тестах.**
  См. блок диагностики выше (P3-9) — правило то же и здесь.

**Блок решений** (P3-4, P3-16, P3-20). Что унести дальше:

- **«сделай как у соседа» — не всегда правильная симметрия.** У P3-4 предложено
  было повторить `..scheduleFrame()` из `runOutsideFrame`. Нельзя: тот зовут вне
  кадра, а `_currentBuildPass()` — из сборки, и `scheduleFrame()` изнутри кадра
  заказывает следующий, то есть лишний пустой кадр после каждого кадра, что-то
  построившего. Кадр теперь заказывается только при `SchedulerPhase.idle`;
- **строка под `yield` выполняется только после того, как потребитель забрал
  событие.** Это ответ на P3-16 и правило, которое стоит помнить, читая любой
  `async*`: отмена на `yield` заканчивает тело, не доходя до следующей строки, а
  войти между `yield` и её выполнением снаружи нельзя — возобновление генератора
  микротаск, а дерево меняется в кадре. Значит приём «`handedOver = true` сразу
  после `yield`», которому учат три темы, надёжен, а комментарий контроллерного
  семейства ошибался в обосновании (вывод у него был верный);
- **«недостижимо через публичный путь» проверяется публичным путём.** P3-20
  утверждала, что `ScopeDependencyDisposalCancelled` наблюдать нельзя. Можно:
  `dispose()` публичен, `root` отдаётся наружу, вызывающий сам ведёт разбор и
  сам его отменяет. Тест написан, класс остался, условие записано в дартдок и в
  тему. Прежде чем спрашивать владельца про удаление публичного класса, стоит
  попробовать до него дойти.

### Волна 14: что сделано и что дальше

Идёт **блоками по родству, а не по номерам находок** — так решено потому, что
три P1 из семи живут в одной поверхности решения, два в разборе жизненного
цикла, два в типах. Состояние на сейчас:

| | что | коммиты | отчёт |
| --- | --- | --- | --- |
| ✔ шаг 0 | P3-2, leak-трекер | `44ef5b3`, `e92367f` | `2026-08-17[4]` |
| ✔ блок B | P1-1, P1-2 | `4d58ddd`, `ecd6d97` | `2026-08-17[5]` |
| ✔ вне очереди | P2-8 (порядок `unmount` в группе) | `232a401` | `2026-08-17[5]` |
| ✔ вне ревью | пункт бэклога: правило `unmount`/`dispose` в теме | `d3b5a6b`, `19ecde3` | — |
| ✔ блок навигации | P1-3, P1-4, P1-7, плюс обход внешнего `PopScope` вне ревью | `b5d5b90`, `65a7de1`, `a3997e5`, `02a4949` | `2026-08-17[6]` |
| ✔ блок типов | P1-5, P1-6 | `baeba7f`, `ea7c6f0` | `2026-08-17[7]` |
| ✔ вечный лоадер | P2-1, P2-2 | `e4aeb98` | — |
| ✔ таймеры | P2-3, P2-4 | `223e2ca` | — |
| ✔ потерянные отказы | P2-5, P2-9 | `3462b71` | — |
| ✔ release-слепота | P2-7 | `2c615ef` | — |
| ✔ обещания документации | P2-10, P2-15, P2-16 | `be5c27f` | — |
| ✔ выход из ошибки | P2-6 (выбор владельца) | `971ff61` | — |
| ✔ витрина и обещание миксина | P2-11, P2-14 | `7fdbefc` | — |
| ✔ обязательный билдер | P2-12 | `8c96c81` | — |
| ✔ покрытие мутаций | P2-13 | `e370e95`, `a85e8e4`, `1e856db`, `987c49d` | — |
| ✔ вне ревью | дефект контроллера, найденный матрицей | `fb385dd` | — |

**Все семь P1 и все шестнадцать P2 третьего ревью закрыты.** Отчётов у
девяти последних блоков нет: правки короткие, а всё, что стоит унести, лежит
вердиктами в самом ревью. Дальше — двадцать P3, см. «Что дальше». **Окно для переименований
из бэклога открыто:** P1-5 уже ломающий, так что в тот же релиз они стоят ноль
сверху. Это решение владельца, а не работа по списку — предложить стоит, но
брать без его слова нельзя.

Ниже — то из волны 14, что придётся знать дальше.

### Шаг 0: leak-трекер (P3-2)

Владелец выбрал начать волну 14 не с P1, а с leak-трекера: два P1 из семи —
утечки (P1-1 держит подписку вечно, P1-5 теряет всё дерево зависимостей), а
регламент требует падающий тест раньше исправления. Разбор — отчёт
`2026-08-17[4]-leak-tracker-report.md` (`44ef5b3`), вердикт стоит в самой
находке P3-2.

**Трекер включён и остаётся включённым.** `test/flutter_test_config.dart` зовёт
`LeakTesting.enable()` — это единственное место, откуда он включается на всю
сьюту; `leak_tracker_flutter_testing` поднят из транзитивной зависимости в явную
dev-зависимость (`^3.0.9`). Сам шаг 0 не добавил ни одного теста — он добавил
проверку поверх всех, и сьюта осталась зелёной целиком (293 теста тогда, 363
сейчас).

**Утечек пакета трекер не нашёл ни одной**, и это проверено пробой. Первый
прогон дал 8 падений в `(tearDownAll)` восьми файлов, и все восемь — свойство
тестов, а не кода. Ключевой факт, который стоит помнить, берясь за любой тест
со скоупом:

> **разбор скоупа асинхронный, а окно проверки leak_tracker закрывается в конце
> теста.** `_model.dispose()` стоит в `finally` у `_performAsyncDispose`
> (`async_scope_core.dart:793`), то есть в самом конце разбора, а
> `pumpAndSettle` двигает только фейковые часы. Тест, который убрал скоуп из
> дерева и на этом кончился, оставляет разбор на полпути — и трекер это видит.

Отсюда образец, которым чинится любой такой тест:

```dart
await tester.pumpWidget(const SizedBox.shrink());
await settle(tester, until: () => log.contains('state.disposeStateAsync'));
```

**Трекер нагруженный** (проверка откатом по §8): мутация, убирающая
`_model.dispose()`, роняет **11 файлов** и печатает 140 отчётов. Файл
восстанавливался копией, `git diff lib/` пуст.

Решение владельца было — **дотянуть разбор в каждом протекающем тесте**, общего
игнора не заводить. Применено: тронуто 23 теста в 8 файлах, **14 получили
разбор, 9 — точечное исключение** `experimentalLeakTesting: unmountableTree`.
Настройка и её обоснование живут в новом `test/utils/leaks.dart`, на месте
вызова стоит одна строка со ссылкой туда.

Исключение — не заглушенная проверка, а последствие фреймворка, и оно опирается
на новую граблю, записанную ниже: **бросок из `didUpdateWidget` оставляет
сломанное поддерево неразмонтированным навсегда.** Все девять исключённых
тестов нарочно роняют нарушение при обновлении элемента («cannot change»,
«cannot go from .value to the owning constructor» и соседние).

~~**Открыто и не сделано:** в сьютах примеров трекер не включён.~~ **Закрыто
блоком инфраструктуры волны 15** (`c625776`): у `example/scopo_demo` и
`example/navigation_node` теперь свои `test/flutter_test_config.dart` той же
формы, что корневой, и `leak_tracker_flutter_testing` в dev-зависимостях. Все
19 тестов зелены — то есть примеры не текут; проверка нагруженная: брошенный
`ValueNotifier` в выбрасываемом тесте краснит оба сьюта, по восемь отчётов
каждый. В гейт §6 эти сьюты по-прежнему не входят, но **их гоняет CI**.

### Блок B: P1-1 и P1-2, и что из них следует дальше

Разбор — `2026-08-17[5]-p1-lifecycle-report.md`. Три вещи оттуда, которые
понадобятся снова:

- **`_isRebuilding` вместо `isBuilding`.** Правка P1-2 намеренно не пошла через
  готовый `runOutsideFrame`: его слепота в release была тогда открытой P2-7, и
  через него P1-2 воспроизвёлся бы в release. Признак «эта сборка идёт сейчас»
  взят своим полем элемента, и остаётся своим полем: P2-7 закрыта, но `isBuilding`
  отвечает про **любую** сборку, а вопрос здесь — про эту. Берясь за что-то похожее, спрашивай
  себя тот же вопрос: одинаково ли это работает в release;
- **тест, написанный по факту поведения, консервирует расхождение.** Тест
  порядка `unmount`, заведённый при P1-1, закреплял прямой порядок — то есть
  ровно то, на что жаловалась P2-8. И вторая версия теста на отсрочку P1-2
  проверяла ноль: зависимый перестраивался сам и читал свежее значение без
  всякого уведомления. Оба раза поймала мутация, а не чтение;
- **сосед бывает не «такой же дефект», а «такая же дыра в покрытии».** У P1-1
  контроллерное семейство оказалось покрыто (мутация роняет шесть тестов), а
  уровень самого скоупа — не покрыт вовсе: `unmountScope()` можно было закрыть
  условием, и вся сьюта оставалась зелёной.

### Блок навигации: P1-3, P1-4, P1-7 и обход внешнего `PopScope`

Разбор — `2026-08-17[6]-navigation-block-report.md`. Что понадобится снова:

- **`PopScope.canPop` — значение времени сборки, а спрашивают его вне сборок.**
  Открытие `Drawer` не меняет ни одного стека и не рассылает ничего
  (`addLocalHistoryEntry` → `changedInternalState` → только `setState`), так
  что узел отвечал из времени до ящика и отдавал наружу весь маршрут. Лечится
  **своей `PopEntry`** вместо виджета `PopScope`: её `canPopNotifier.value`
  считается в момент, когда спрашивает `ModalRoute.popDisposition`;
- **маркер в локальной истории нельзя отличить от чужой записи.** Маршрут
  сообщает только, пуста ли история (`willHandlePopInternally`), счётчика нет.
  Поэтому `_HookEntry` убран совсем, а две его работы разнесены: вид стрелки
  `AppBar` — своя страница с `impliesAppBarDismissal`, действие стрелки —
  `NodeNavigatorState.maybePop`, уходящий из узла (`BackButton` зовёт именно
  `maybePop`, не `pop`);
- **`previous.maybePop()` из `_popOutside` вешает сьюту.** Своя `PopEntry`
  узла зарегистрирована на том маршруте, о котором спрашивают: навигатор выше
  возвращает нажатие обратно, узел решает снова, без конца. Проверено
  запуском;
- **`ValueListenable` не экспортируется из `package:flutter/material.dart`** —
  нужен явный `package:flutter/foundation.dart`. Ошибка при этом невнятная:
  «Classes and mixins can only implement other classes and mixins»;
- **спросить маршрут дешевле, чем сказать ему.** Проброс читает
  `route.popDisposition` — это не рассылает ничего. `previous.maybePop()` на том
  же месте сообщает отказ каждому `PopScope` на маршруте, и приложение слышит
  одно нажатие как два. Измерено обеими версиями, таблица — в отчёте
  `2026-08-17[6]`;
- **опасение, записанное здесь до измерения, было неверным.** Я думал, что
  проброс через маршрут начнёт закрывать приложение с первого маршрута. Не
  начинает: вызов вложен в тот `maybePop`, который уже ответил «обработано». Это
  и есть цена «показать, а не описать» — владелец попросил измерить, и
  измерение сняло возражение.

### Блоки P2: вечный лоадер и таймеры

Отдельных отчётов нет — разбор лежит вердиктами в самом ревью. Три вещи,
которые пригодятся дальше:

- **фейковые часы не достают до таймера корневой зоны, и теперь это касается
  всех четырёх лимитов** — `scopeKey`, отмена инициализации, `disposeScope`,
  ожидание детей. Тест, которому надо дождаться истечения, ждёт по настоящему
  времени (`settle`, а не `pump(duration)`). Исключение — `pauseAfterInitialization`:
  она нарочно осталась в текущей зоне, её видит пользователь;
- **`finally` генератора держит не только ресурс, но и отказ.** Ниже по потоку
  отказ инициализации виден не раньше, чем закончится генератор, поэтому любой
  неограниченный `await` в его `finally` — это «скоуп грузится вечно», а не
  «утекла зависимость». Так было в `ScopeAutoDependencies`; в теме `AsyncScope`
  это теперь сказано и про рукописную защиту;
- **тест на «висит», а не на «упало», пишут со своим таймаутом.** У теста P2-2
  стоит `timeout: Timeout(Duration(seconds: 10))`: иначе красный приходит через
  тридцать секунд дефолта, и в прогоне это выглядит как зависшая сьюта.

### Блок потерянных отказов: P2-5 и P2-9

Отдельного отчёта нет — разбор лежит вердиктами в самом ревью. Правило теперь
одно на весь разбор: **первый отказ уходит броском, каждый следующий — в
`FlutterError.reportError`** через новый приватный `_reportFailure`
(`base/base.dart`). Что стоит унести дальше:

- **`ScopeAutoDependencies` наверх ничего не отдаёт.** Его `dispose()` репортит
  то, что бросили дети, и сам не бросает никогда, а `onUnmount()` — рефроусит.
  Поэтому асинхронная половина P2-9 через штатный контейнер не воспроизводится
  вовсе, и оба теста написаны на рукописной реализации `ScopeDependencies`
  (`_PlainDepScope` в `cleanup_after_user_error_test.dart`). Первая проба на
  `ScopeAutoDependencies` показала это сразу: отказ диспозера не всплыл нигде;
- **правка сломала существующий тест, и это был тест-консерв.**
  `lite_scope_test.dart` «reports the first failure of a teardown, not the last»
  спрашивал `tester.takeException()` ровно об одном зарепорченном исключении —
  то есть закреплял ровно ту потерю, о которой P2-5. Стало два, тест переписан
  на сбор через `FlutterError.onError`. Берясь за находку про «теряется
  молча», жди такого: сьюта может держать потерю на месте.

### Блок покрытия: P2-13

Отдельного отчёта нет — вердикт в самом ревью. Четыре вещи, которые стоит
унести:

- **тест на проводку параметра пишут по эффекту в дереве, а не по значению
  геттера.** Матрица утверждает очередь за `scopeKey`, истечение через 50 мс
  вместо глобальных трёх секунд, придержанную готовую ветку — то, что видно
  снаружи скоупа. Геттер, возвращающий правильное число и никем не читаемый,
  прошёл бы тест, написанный наоборот;
- **зависимого, которого перестраивает родитель, нельзя спрашивать о
  подписке.** Первая версия теста на `paramsOf(listen: true)` прошла **на
  мутации, игнорирующей `listen`**: читатель был `Builder`, и его перестраивал
  родитель. Лечится `const`-читателем со счётчиком сборок — приём уже был в
  том же файле, в тесте `ScopeWidgetBase`;
- **два скоупа на одном ключе требуют пустого кадра между ними.** Оба —
  виджеты одного типа, и подстановка второго на место первого обновляет тот же
  элемент: очереди нет вовсе, а тест этого не видит. Между ними должен быть
  кадр, где нет ни одного;
- **непокрытая проводка прячет не только мутации.** Тест матрицы на
  `initCancellationTimeout` в контроллерном семействе нашёл живой дефект —
  контроллер, чья инициализация проснулась после конца разбора, не
  освобождается вовсе; записан в «Открыто, вне списка ревью».

### Блок release-слепоты: P2-7

Отдельного отчёта нет — вердикт в самом ревью. Главное, что стоит унести:

- **не-ассертного признака идущей сборки у Flutter нет.** `BuildOwner.debugBuilding`
  и `WidgetsBinding.debugBuildingDirtyElements` — оба внутри `assert`, а флаг,
  который `buildScope` держит сам (`_scheduledFlushDirtyElements`), приватный.
  Поэтому «дать `isBuilding` release-видимый источник правды» — это не найти
  такой источник, а **завести свой**: пакет считает собственные пересборки
  (`_scopeRebuildDepth`, обычное поле, счётчик — скоупы вкладываются). Ответ
  стал полнее, но остался неполным, и это записано в дартдоке и в теме `utils`,
  а не замолчано;
- **тест на release-поведение в debug невозможен, поэтому проверяют механизм.**
  `isBuilding` в debug отвечает `true` из ассертного флага независимо от правки —
  тест на него доказывает ноль. Тест читает `scopeIsRebuilding` прямым импортом
  из `src/`, на сборке вне кадра.

### Блок типов: P1-5, P1-6

Разбор — `2026-08-17[7]-type-block-report.md`. Одно, что стоит унести дальше:

- **самотипный параметр не даёт типа `Self`.** `T extends Base<T, C>` ловит
  «здесь стоит не наследник», но **не** ловит «здесь стоит чужой наследник»:
  `_Wrong extends Base<_Right, C>` удовлетворяет ограничению. Направление
  находки P1-5 состояло ровно в этом ограничении, и прогон показал, что оно
  дефект не закрывает. Закрыла проверка `this is! T` в начале `init` — до того,
  как что-либо построено. **Правило: F-ограничение проверяют запуском, а не
  чтением;** выглядит оно убедительнее, чем работает.

### Волна 14, попутно: две инициализации `Scope` описаны в теме

Не находка ревью, а вопрос владельца из бэклога: «`dispose`/`disposeStateAsync`
вызывается не всегда — а какое правило у `unmount`/`onUnmount`?» Записано в
`doc/full_scope.md`, раздел «Two initializations, and what a failure of each
leaves behind», с зеркалом в `docs/ru/`.

Ключевое, что стоит помнить: **у `Scope` две инициализации, и провал каждой
оставляет разное.** Контейнер (`initDependencies`) и собственная асинхронная
половина состояния (`ScopeState.initStateAsync`). Проверено пробой:

```
готов:            state.onUnmount, dep.unmount b, dep.unmount a,
                  state.disposeStateAsync, dep.dispose b, dep.dispose a
провал deps:      dep.unmount b, dep.unmount a, dep.dispose b, dep.dispose a
провал initStateAsync:
                  state.onUnmount, dep.unmount b, dep.unmount a,
                  dep.dispose b, dep.dispose a
```

**`dep.unmount` идёт в обратном порядке** — это P2-8, закрытая вне очереди; до
неё шёл в прямом, и две половины одного разбора расходились.

- **провалился `initDependencies` — состояния не существует вовсе.**
  `createState()` выполняется при постройке готовой ветки, а её не построили;
  выполнять `ScopeState.onUnmount`/`disposeStateAsync` не на чем;
- **провалился `initStateAsync` состояния — состояние есть, его отмонтируют, но
  не утилизируют.** `onUnmount()` идёт, `disposeStateAsync()` — нет: у состояния свой
  `_initSucceeded`, и `LiteScopeCoreState._performAsyncDispose` выходит раньше;
- **зависимости возвращают на любом пути**, но при провале `initDependencies`
  — не элементом: контейнер разбирает себя сам, изнутри инициализации, по
  `autoDisposeOnError`. Элементу контейнер вручают только вместе с
  `ScopeReady`.

Тремя темами половина этого раньше описывалась (`async_scope`,
`async_data_scope`, `async_controller_scope`), а темой `full_scope` — нет: её
список из шести шагов читался как безусловный и не различал, о какой из двух
инициализаций речь. Заодно записано, почему семейства отвечают по-разному, хотя
правило одно: **хук, написанный про законченную вещь, на незаконченной не
выполняется; вещь, которую скоуп держит за вас, возвращается что бы ни
случилось.**

**Таблица темы закреплена тестами** — два новых в
`test/scope_lifecycle_order_test.dart`, по одному на провальный путь; готовый
путь там был закреплён и раньше. Оба проверены мутацией.

**У каждой находки первых двух ревью в конце стоит вердикт** — что с ней
сделано, в какой волне и каким коммитом, и чем находка ошибалась, если
ошибалась. Правило владельца, записано в `AGENTS.md` §4. Прозу находки при этом
не правят: ревью исторично. **У находок третьего ревью вердиктов пока девять** —
у всех семи P1, плюс P3-2 и P2-8, закрытых волной 14; остальные пишут по мере
закрытия, туда же, в файл `2026-08-17[3]`.

Главный вывод **второго** ревью, тот же по смыслу: **три P1 из шести — это тот
же дефект, что уже исправляли в 0.10.0, но в соседней ветке кода.** Падение
диспозера чинили в последовательной группе и не посмотрели в параллельную;
падение пользовательского хука закрывали в `disposeScope` и не закрыли в
`onUnmount` десятью строками выше; девятку параметров пробросили в новом
семействе и не пробросили в двух старых. Отсюда правило на будущее: **закрыл
находку — найди её же в соседней реализации** и запиши результат поиска в
отчёт волны.

Отчёты по волнам, по одному на волну. **Номера находок в волнах 1–9 — из
первого ревью (`2026-08-14[10]`), в волнах 10–12 — из второго
(`2026-08-16[12]`), а начиная с волны 14 будут из третьего
(`2026-08-17[3]`).** Нумерация во всех трёх независимая, так что «P1 №2» без
указания ревью ничего не значит. Три волны идут не по спискам ревью вовсе:
восьмая, девятая (новое семейство по бэклогу) и тринадцатая (пробелы в
покрытии):

| волна | находки | отчёт |
| --- | --- | --- |
| 1 | P1 №3, P2 №4, №5 | `2026-08-16[1]-cleanup-completeness-report.md` |
| 2 | P2 №8, №9 | `2026-08-16[2]-scope-data-and-selectors-report.md` |
| 3 | P2 №11, №12, №13, P3 №18 | `2026-08-16[3]-listenable-utils-report.md` |
| 4 | P2 №6, №7, №10 | `2026-08-16[4]-lifecycle-contracts-report.md` |
| 5 | P2 №14, P3 №17 | `2026-08-16[5]-navigation-node-async-onpop-report.md` |
| 6 | P3 №16 | `2026-08-16[6]-scope-notifier-value-tag-report.md` |
| 7 | P2 №15 | `2026-08-16[7]-init-cancellation-timeout-report.md` |
| 8 | вне списка ревью | `2026-08-16[8]-dispose-async-timeout-report.md` |
| 9 | бэклог: `AsyncControllerScope` | `2026-08-16[11]-async-controller-scope-report.md` (дизайн — `[9]`, план — `[10]`) |
| 10 | шесть P1 повторного ревью | `2026-08-16[13]-p1-fixes-report.md` (ревью — `[12]`) |
| 11 | семнадцать P2 повторного ревью | `2026-08-16[14]-p2-fixes-report.md` |
| 12 | двадцать P3 повторного ревью + четыре записи бэклога | `2026-08-17[1]-p3-fixes-report.md` |
| 13 | не по списку ревью: семь пробелов в покрытии | `2026-08-17[2]-coverage-gaps-report.md` |
| 14 | третье ревью, шаг 0: P3-2 (leak-трекер) | `2026-08-17[4]-leak-tracker-report.md` |
| 14 | третье ревью, блок B: P1-1, P1-2, плюс P2-8 вне очереди | `2026-08-17[5]-p1-lifecycle-report.md` |
| 14 | третье ревью, блок навигации: P1-3, P1-4, P1-7 | `2026-08-17[6]-navigation-block-report.md` |
| 14 | третье ревью, блок типов: P1-5, P1-6 | `2026-08-17[7]-type-block-report.md` |

P1 №1 и P1 №2 **первого** ревью закрыты раньше волны 1, вместе с ревью-волнами
над ними: записи `2026-08-15[1]`–`[3]`, `[5]`–`[10]`, `[12]`.

### Решения владельца, которые действуют дальше

- смена конструктора между владеющим и `.value` у `ScopeModel`/`ScopeNotifier`
  **запрещена ассертом**, а не переносится (волна 4);
- `State.dispose` **закрыт от переопределения** у `LiteScopeState`/`ScopeState`;
  синхронный разбор идёт в `onUnmount()` (волна 4);
- правило имён: **присваиваемый колбэк — короткий глагол** (`dep.unmount`,
  `AsyncScope(unmount:)`), **переопределяемый хук — `onX`**;
- скриншоты для pub.dev не добавляем: статичные кадры технического демо не
  объясняют ценность инфраструктурного пакета и не дают pub points;
- **в русских зеркалах комментарии внутри примеров кода тоже по-русски**;
  исключение — директивы `// ignore: …`, это код, который читатель копирует к
  себе (правило записано в `AGENTS.md` §7);
- **ссылки на темы из `README.md` ведут в репозиторий**
  (`github.com/vi-k/scopo/blob/main/doc/*.md`), а не на
  `pub.dev/documentation/scopo/latest/topics/…`: пока 0.10.0 не опубликована,
  вторые не открываются вовсе, а из репозитория до `doc/` иначе не добраться.
  После публикации ссылки на pub.dev станут рабочими — менять их обратно или
  нет, решать владельцу;
- **у корневого `NavigationNode` хук `onPop` спрашивают, но наружу не
  выходят** (волна 13): `true` больше ничего не забирает, потому что корневому
  узлу пропускать поп некуда. Хук остаётся ради самого нажатия — «нажмите ещё
  раз для выхода», собственный `SystemNavigator.pop()`;
- **защита написанной руками инициализации — `finally` с флагом, а не
  `catch`** (волна 13): отмена не бросает ничего, поэтому `catch` не
  срабатывает. Так переписаны примеры в темах `AsyncScope`, `AsyncDataScope` и
  `Scope`; в `ScopeModel` остаётся `catch` — `create` синхронный;
- пакет не публиковать без явной просьбы.

## Проверки

Полный гейт из восьми команд `AGENTS.md` §6 пройден целиком **2026-08-22, на
голове `main`**, на закреплённом тулчейне (Flutter 3.27.0 / Dart 3.6.0 через
fvm, он же заявленный пол), включая сьюты обоих примеров. **Тесты идут с
включённым leak-трекером** — теперь и в примерах, см. раздел о волне 14 выше.

**Тот же гейт с этого момента гоняет машина** —
`.github/workflows/ci.yml`, на `push` в `main`, на любой pull request и по
кнопке (`workflow_dispatch`). Шаги те же и в том же порядке, плюс сьюты
примеров и `git diff --exit-code` в конце. Локальный прогон это не отменяет:
CI отвечает через несколько минут и на чистом клоне, а `docs/backlog.md` с
правкой владельца в дереве ему не мешает — pubignore-нутые файлы
`pub publish --dry-run` не считает.

Восемь команд смотрят на `lib/`, `test/`, `example/*`, `README.md`, `doc/`,
`docs/ru/` и корневые файлы пакета. Коммит, который не тронул ничего из этого —
а `docs/handoff.md`, `docs/backlog.md`, `docs/records/` и `AGENTS.md` сюда не
входят, — гейта не касается вовсе, и пересчитывать из-за него нечего. **Тронул
что-то из списка — гейт надо прогнать заново**, даже если это одна строка в
`README.md`.

Второе ревью проходило на `9296648`, третье — на `72bd596`, четвёртое — на
голове `main` того дня. То есть **и 43, и 44, и 52 находки получены на зелёном
гейте**: ни одна из них гейтом не ловится. Это стоит помнить, решая, что
считать достаточной проверкой.

| проверка | результат |
| --- | --- |
| `fvm flutter test` | **451 тест, все зелёные, leak-трекер включён** |
| `fvm flutter analyze` (корень) | `No issues found!` |
| `analyze` во всех трёх `example/*` | `No issues found!` в каждом |
| `fvm dart format --set-exit-if-changed lib test` | 99 файлов, 0 changed |
| `fvm dart doc --dry-run` | 0 warnings, 0 errors |
| `fvm dart pub publish --dry-run` | 0 warnings |
| `sh docs/ru/check.sh` | переводы актуальны: 16 |

Архив пакета — **239 КБ** сжатыми. Сьюта в него не входит с волны 24 (L17):
756 КБ против 540 КБ у `lib/`, качает их каждый, кто зависит от пакета, а
документация — это темы в `doc/`, и они едут.

**В гейт §6 не входит:** свои тесты есть у двух примеров — `example/scopo_demo`
(3 теста) и `example/navigation_node` (17). Оба прогнаны вручную и зелены — и
прогонять их стоило: волна 13 меняла `NavigationNode`, а гейт этих
сьют не видит. **Тронул `lib/` — прогони и их**, `fvm flutter test` из каждой
папки. Один тест демо импортирует папку демо по пути, так что переименования
его задевают. С волны 15 их гоняет и CI, но локально это по-прежнему на тебе.

Две ловушки самого гейта:

- `dart doc` и `pub publish` запускать **через `fvm dart`**, а не прямым бинарём
  из `~/fvm/versions/...`: без обёртки `dart doc` падает на
  `type 'Null' is not a subtype of type 'String'` в `DartdocOptionContext.sdkDir`;
- `dart format --set-exit-if-changed` **и пишет, и возвращает код**. Первый
  прогон после правок может вернуть 1, отформатировав файл; повторный даёт 0.
  Это не отказ гейта, но проверять надо повторным прогоном.

Тот же набор проходил и на прежних полах: на 3.29.0, пока он был полом, и на
глобальном 3.44.9 до него.

## Грабли, на которые не стоит наступать второй раз

**Ловушки тестов, найденные 2026-08-20.** Все четыре стоили времени, и все
четыре повторимы:

- **Дерево нельзя разворачивать целиком** (`pumpWidget(const SizedBox())`) в
  тесте, где разбор бросает или где родитель бросил ребёнка: отказ вылетает из
  `Element.unmount()`, обход фреймворка на этом кончается, и leak-трекер ловит
  неутилизированный `_UbiquitousInheritedElement` или
  `SingleChildRenderObjectElement`. Забирать надо только сам скоуп, оставив
  `Directionality` над ним, — так делает соседний существующий тест.
- **`hang.complete()` в теле теста конфликтует с
  `addTearDown(hang.complete)`** — `Bad state: Future already completed`. В
  `addTearDown` нужен `if (!hang.isCompleted)`.
- **Тест, который ждёт, что чего-то НЕ произойдёт, должен снимать предел, а не
  надеяться на скорость.** Идиома `settle(tester, until: () => false)` даёт
  честный шанс, но если рядом висит невыключенный таймаут по умолчанию, упадёт
  соседнее ожидание, а не проверяемое.
- **События single-subscription `StreamController`, созданного в теле
  widget-теста, не доставляются одним `pumpAndSettle`** — нужен
  `tester.runAsync(...)`. Broadcast-контроллер и `async*` доставляются
  нормально. На этом ревьюер чуть не выписал фантомный дефект.


Найдено по ходу волн, стоило времени, в коде не видно:

- **внешний `catch` в `_performAsyncInit` выполняется внутри
  `performRebuild`.** Отказы, ради которых он существует (поиск координатора,
  не-генераторный `initScope`), происходят **до первого `await`**, поэтому
  прямой `_model.update` оттуда дёргает `markNeedsBuild` посреди сборки
  элемента. Применять состояние только через
  `SchedulerBinding.instance.runOutsideFrame`. И `_initCompleter` завершать
  раньше всего, что может бросить: иначе `_performAsyncDispose` паркуется на
  нём навсегда, и сьюта не падает, а **висит** (волна 10);
- **ошибка внутри `yield*`-делегированного потока идёт прямо подписчику** —
  `try/catch` в генераторе её не видит, нужен явный `await for`;
- **ошибка, проброшенная из `_performAsyncDispose()`, до
  `tester.takeException()` не доходит**: метод вызывается на отброшенном
  future, поэтому такие тесты оборачивают в `runZonedGuarded`;
- **ассерт в `didUpdateWidget` рвёт кадр**: обновление бросается на середине и
  следом прилетает вторичный ассерт фреймворка (`'_dependents.isEmpty'`).
  Ставить такие проверки в `build`, где их ловит build error boundary;
- **у того же броска есть второе последствие: он утекает поддерево.** Бросок из
  **обновления** оставляет сломанное поддерево неразмонтированным навсегда —
  виджетов в дереве уже нет, вторичного исключения нет, а элементы и
  render-объекты ниже разрыва не разбираются никогда, и убирание дерева до них
  не доходит. Бросок из `build` так не делает: там дерево размонтируется как
  обычно. Проверено пробой на двадцати строках чистого Flutter, без scopo
  (волна 14). Отсюда девять тестов сьюты, у которых стоит
  `experimentalLeakTesting: unmountableTree`;
- **`close()` снимает скриншот поддерева**, поэтому фикстура для теста с
  `close()` должна иметь ненулевой размер, а не `SizedBox.shrink()`. И по той
  же причине **вложенный скоуп не удерживает родителя через `close()`**:
  `ScreenshotReplacer` заменяет поддерево картинкой, ребёнок размонтируется и
  снимается с учёта. Чтобы ожидание детей истекло, у ребёнка должен зависнуть
  собственный разбор (волна 11);
- **три копии `_settle` в тестах ведут себя по-разному.** Общий
  `test/utils/settle.dart` двигает и реальное, и фейковое время
  (`runAsync` + `pump(step)`), а приватная копия в `lite_scope_test.dart`
  делает `pump()` без длительности — фейковые часы стоят, и таймер зоны
  (`Future.timeout` внутри `waitForChildren`) не срабатывает никогда. Тест на
  такой таймер надо писать на общем помощнике (волна 11);
- **таймер, заведённый на разборе, роняет чужие виджет-тесты.** Лимит через
  `Future.timeout` заводит таймер зоны сразу, а под тестовым биндингом отмена
  подписки не завершается вовсе — таймер оставался непогашенным к концу любого
  теста с живым скоупом (`A Timer is still pending`, 17 падений). Лимиты на
  такие ожидания брать из корневой зоны: `Zone.root.createTimer` (волна 7);
- **у Flutter нет хука «зависимый начинает сборку»**: `Element.dirty` во время
  `build()` всегда `true`, идентичность `widget` не ловит `setState`, у
  `InheritedElement` подходящего хука нет. Отсюда граница сборки по кадру в
  `scope_widget_core.dart`;
- **`git checkout -- <файл>` не отменяет мутацию, а стирает по этому файлу всю
  несохранённую работу.** Волна 12 потеряла так `hasData` и охрану второго
  `ready` в `async_data_scope_core.dart`. Для мутаций годится
  `git stash push -- <файл>` и `git stash pop`; мутацию, внесённую скриптом,
  снимать обратным скриптом. **Ещё надёжнее — вообще не мутировать рабочее
  дерево:** третье ревью гоняло 19 мутаций и 12 проб в копии репозитория,
  распакованной за пределами проекта (`tar` без `.git`, `build`, `docs`).
  Копия видит тот же `fvm`, `pub get` не нужен, сьюта на ней зелёная сразу.
  Так рабочее дерево остаётся чистым, а мутацию не надо снимать вручную;
- **ветка `_debugIsDescendantOf` в `markNeedsBuild` — разрешающая, а не
  бросающая.** `markNeedsBuild()` на элементе, который *сам сейчас строится*,
  ассертом Flutter **не ловится** (`return true` внутри `assert`), а дальше
  глотается через `if (dirty) return;`. Отсюда P1-2 третьего ревью. Не
  рассчитывай, что фреймворк предупредит;
- **своя подмена `FlutterError.onError` глотает провал собственного теста.**
  Тест, собирающий репорты в список на всё своё тело, ловит туда же и
  `TestFailure` от упавшего `expect`: прогон не краснеет, а **виснет**.
  Возвращать обработчик надо до проверок, а не только тир-дауном (волна 13);
- **прерванный `flutter test` оставляет живой `flutter_tester`**, и следующий
  прогон висит на нём, ничего не печатая. Снимать процесс руками (волна 13) —
  **но не `pkill -f flutter_tester` вслепую.** На этой машине параллельно идут
  прогоны других проектов (`~/development/tez/ttm-driver-speed`, SDK 3.44.4,
  запускается через `melos exec --concurrency 10`), и такой pkill рубит живой
  чужой прогон. Проверено дорого: 2026-08-17 я так и сделал — процессы выглядели
  зависшими, владелец подтвердил «хозяев не осталось», — а они появились снова с
  новыми pid, потому что `melos` был жив и запускал их заново. То есть **и
  «висит давно», и слово владельца не доказывают, что процесс бесхозный:
  доказывает только родитель.** Смотреть `ps -eo pid,ppid,etime,args`, найти
  ppid тестера и убедиться, что это не работающий раннер; свои узнаются по
  версии тулчейна (`3.27.0`; до 2026-08-18 это была `3.29.0`) и по `my/scopo` в
  аргументах, чужие — по `3.44.4` (волна 14). **Тот же зверь, но другая грань:
  второй свой же прогон `fvm flutter test` параллельно первому по тому же
  дереву не просто оставляет сироту — он портит результат.** Два процесса
  делят один `.dart_tool`/`build`, и от гонки изолятов за общий кеш сборки
  прогон может показать событие, которого код заведомо не производит (так
  простой `LiteScope` без своего хука отчитался `onReady`). И отдельно: PID,
  который харнесс отдаёт как результат запуска, — это обёртка
  (`flutter_tools.snapshot test`), а не сам движок; она заводит отдельный
  `flutter_tester` на каждый тестовый файл, и убийство только внешнего PID
  сироту не снимает. Снимать нужно всё поддерево процессов — тем же
  `ps -eo pid,ppid,etime,args` — и следом `fvm flutter clean && fvm flutter
  pub get`, прежде чем доверять следующему прогону (задача 2 плана
  наблюдателя, 2026-08-19);
- **`IconButton.tooltip` съедает длинное нажатие, повешенное снаружи.**
  `IconButton` строит `Tooltip` **внутри себя**, над своим `InkWell`, а у
  `Tooltip` есть собственный `LongPressGestureRecognizer`. Обёртка
  `GestureDetector(onLongPress:)` снаружи кнопки оказывается выше него, её
  таймер стартует позже, арену забирает подсказка — и внешний обработчик не
  зовётся ни разу. Лечится подъёмом подсказки: `Tooltip` → `GestureDetector` →
  `IconButton` без `tooltip:`. Поймано тестом при переносе с
  `IconButton.onLongPress` на 3.27 — глазами в коде этого не видно, а гейт
  сьюты примеров не гоняет (2026-08-18);
- **публикатор логов в тесте пишется через `implements`, и только так.**
  `CustomLogPublisher` — `abstract interface class`, наследовать его нельзя
  (`extends` даёт ошибку компиляции). Фабрика `ScopeLogPublisher(fn)` тоже не
  выручает: в `const`-контексте анализатор теряет аргумент типа и говорит
  `const_constructor_param_type_mismatch`, а без `const` требует его обратно
  через `prefer_const_constructors`. Рабочий вид — свой `final class … implements
  ScopeLogPublisher` с `const`-конструктором (`test/scope_logger_test.dart`,
  2026-08-19);
- **бросок из `onPopInvokedWithResult` оставляет навигатор запертым.**
  Пользовательский колбэк, который роняет на `didPop: true`, бросает внутри
  `NavigatorState._flushHistoryUpdates`, а `_debugLocked` снимается ассертом в
  конце `pop()` — до него дело не доходит. Дальше любой разбор дерева даёт
  вторичный `'!_debugLocked'` из `NavigatorState.dispose`, так что тест краснеет
  не тем, чем задумывался. Отказ для теста ставить туда, где фреймворк ещё цел
  (волна 15);
- **корневой `flutter analyze` требует, чтобы примеры были фетчены.** Он
  обходит всё дерево, `example/` включая, и на чистом клоне даёт 60 ошибок про
  несуществующие `package:`-URI: пакеты примеров не разрешены. Локально шаг
  проходит только потому, что `pub get` в примерах когда-то делали. То есть
  локальный гейт годами держался на необъявленном состоянии рабочего дерева, и
  показала это первая же попытка прогона CI (волна 15). Порядок в CI:
  `pub get` примеров — **до** корневого анализа;
- **`stamp.sh` штампует и то, чего не правили.** Правка русского зеркала
  прошла, парная правка оригинала — нет (команда упала на разборе шелла), и
  `stamp.sh` записал в перевод хеш неизменившегося оригинала; `check.sh` после
  этого доволен. Смотреть `git status` **до** штампа (волна 12);
- **каскад на классе не работает:** `ScopeConfig..field = value` — ошибка
  компиляции, у `Type` нет сеттеров;
- **каскад в теле стрелки прилипает к выражению, а не к получателю:**
  `dep..unmount = () => log.add(x)..dispose = …` разбирается как
  `log.add(x)..dispose = …`, то есть каскад уходит внутрь лямбды. Ошибка
  компиляции невнятная («The setter 'dispose' isn't defined for the class
  'void'»). Лечится блочным телом или отдельными присваиваниями (волна 14);
- под `AutomatedTestWidgetsFlutterBinding` ошибка, лежащая в буфере потока до
  подписки (`Stream.error`), не проходит через `asyncMap` — а именно так
  подписывается `_performAsyncInit`. Ветку ошибки поднимать `async*`-потоком,
  который бросает после первого события; `Stream.error` — только внутри
  `tester.runAsync`. Разбор:
  `docs/records/2026-08-12[1]-stream-error-under-test-binding-report.md`;
- **два прогона `fvm flutter test` зависли без вывода при работе над
  `ScopeConfig.observer`.** План `2026-08-19[2]`, задача 1: наблюдатель, чей
  `onInit` реентерно зовёт `tester.pumpWidget` из уже строящегося дерева, и
  отдельная проба, подменявшая `FlutterError.reportError` на `print` внутри
  `notifyObserver`. Оба раза `flutter test` не падал и ничего не печатал —
  прогон снимали руками (`TaskStop`). Причина не установлена; в обоих сценариях
  тест держал свою подмену `FlutterError.onError`, как и в грабле про волну 13
  выше — тот ли это механизм, не проверено. Закоммиченный код и тесты задачи 1
  не висят; разбор — `task-1-report.md` в каталоге плана.

## Известные проблемы

### Из ревью

**Все 43 находки повторного ревью закрыты** — шесть P1 (волна 10), семнадцать
P2 (волна 11), двадцать P3 (волна 12). Вердикт по каждой стоит в самом ревью,
разбор — в отчётах `[13]`, `[14]` и `2026-08-17[1]`.

**Все 44 находки третьего ревью закрыты** (`2026-08-17[3]-project-review.md`):
семь P1 и шестнадцать P2 волной 14, двадцать одна P3 волной 15. У каждой в самом
ревью стоит вердикт. Три из них закрыты не так, как предлагало ревью, и это в
вердиктах названо: **P3-3** не подтвердилась (её направление проверено пробой как
ухудшающее), **P3-20** не подтвердилась (состояние достижимо публичным путём), а
у **P3-16** верной оказалась сторона тем, а не комментария кода. Плюс P3-8 —
последствие недостижимо, порядок всё равно поправлен, а рядом закрыт настоящий
дефект.

**Списки всех четырёх ревью исчерпаны: 18 + 43 + 44 + 52.** Четвёртое закрыто
десятью волнами (16–25) — см. «Что дальше», пп. 10–13; у каждой находки вердикт
в `docs/records/2026-08-20[1]-project-review.md`, и там же сказано, чем находка
ошибалась, если ошибалась. **Очереди работ нет.**

Три вещи из четвёртого ревью закрыты не правкой, а разбором, и их стоит знать,
чтобы не открывать заново: **M15** — пропавший `scheduleFrame()` виджет-тесту
невидим, потому что `pump()` рисует кадр безусловно; **M18** и релизная
половина **M5** — за ассертом, а `flutter test` всегда с ассертами; **M12** —
две взаимно-избыточные охраны, каждая делает другую недостижимой, так что
убить тестом можно любую, но не обе. У всех трёх причина записана в коде.

Что из волны 18 стоит помнить как решение: **`NavigationNode.enabled`** —
приложение говорит узлу, что он не тот, кто на экране; сам узел этого узнать не
может. И **`AsyncScopeElementBase.boundsDisposeScopeItself`** — семейство с
двумя стадиями за `disposeScope()` ограничивает их само, потому что один лимит
вокруг обеих пропускает вторую.

Что из волны 17 стоит помнить как решение: **отказ, у которого не осталось
адресата, отчитывается и не бросается** — это и раньше было правилом четырёх
стадий разбора, а теперь оно же действует у `unmount()` элемента и у
`ScopeController`. Броском отказ уходит только туда, где вызывающий есть:
`close()` слышит, фреймворк — нет.

Что из волны 16 стоит помнить как решение: **`ScopeTimeout.none` значит одно и
то же везде, где его пишут**, включая все четыре умолчания `ScopeConfig`, — а
там, где «ждать сколько понадобится» смысла не имеет (`initCancellationTimeout`
у отдельного скоупа, `pauseAfterInitialization` у любого), он отвергается
ассертом, а не наделяется вторым смыслом. И **отрицательный `Duration` теперь
отказ, а не мгновенно истекающий предел** — это цена того, что маркер узнают по
типу: арифметика его теряет, и потерянный он раньше значил ровно обратное.

Третье ревью пересекается с разделом «Открыто, вне списка ревью» ниже в двух
местах, и там это помечено: `previous.pop()`, обходящий внешний `PopScope`, —
это тот же `_popOutside`, что и в P1-3, и лечится той же правкой; а
release-слепота `isBuilding`, записанная там как принятое ограничение, третьим
ревью переоткрыта как P2-7 с конкретным последствием — и закрыта волной 14.

Что из закрытого стоит помнить как решение, а не как правку:

- **подписаться на скоуп можно только из сборки** — теперь это ассерт в
  `ScopeContext._find` и правило в `doc/base.md`. Граница пересборки взята по
  кадру, и подписка из `didChangeDependencies` жила ровно до первой
  пересборки от родителя;
- **`value` у `.value`-конструкторов не-nullable** — `null` перестал
  компилироваться, ассерт для этого не понадобился;
- **`asStream` берёт один аргумент типа вместо двух** — тип контейнера
  выводится, приведение `this as T` исчезло;
- **инварианты семейств запечатаны** (`@nonVirtual` на `initScope`
  data-слоя и `initDataAsync` контроллерного, `@mustCallSuper` на его
  `disposeScope`) — гарантия анализаторная, тестами не покрывается;
- **`data` отвечает раньше, чем модель становится ready, и так и задумано.**
  Окно между `_hasData` и `AsyncScopeReady` закрывать нельзя: `disposeScope`
  читает `data` именно в нём, и обещание «`dispose` всегда получает значение»
  держится на этом. Исправлено обещание, а не код (P3-7);
- **`isBuilding` спрашивает три источника, и только один из них ассертный.**
  `buildScope` бывает и вне кадра — так строит первое дерево `runApp`, — а флаг
  build owner'а живёт внутри `assert`, поэтому в release от него ничего не
  остаётся. Это была P2-7, **закрытая волной 14**: пакет считает свои
  пересборки обычным полем (`_scopeRebuildDepth` в `is_building.dart`), и
  `isBuilding` спрашивает его тоже. Не-ассертного признака чужой сборки у
  фреймворка нет вовсе, так что в release неотвеченной остаётся сборка, не
  принадлежащая ни кадру, ни пакету; это сказано в дартдоке и в теме `utils`;
- **три метода `ScopeController` — односторонняя последовательность**:
  `performInit` после `performDispose` ничего не делает, а разбор один на
  контроллер и его исход видят все вызвавшие;
- **`ScopeConfig.reset()`** возвращает пять глобальных переключателей к
  умолчаниям; наблюдателя намеренно не трогает — раньше по той же причине не
  трогал логгер. Три сьюты пакета им пользуются;
- **у элемента на асинхронной ветке нет достижимого пути потерять
  собственный `disposed`.** `init()` синхронный и идёт одним стеком с
  `mount() → build()`, без точки, где `unmount()` мог бы вклиниться раньше
  конца; провал `init()` ставит `_initPhase` в `failed`, не оставляет в
  `pending`, а error boundary Flutter гасит его на месте потомка, наружу
  через `mount()` родителя не пробрасывая. `unmount()` зовёт `dispose()` (а
  с ним и единственный источник `onDisposed`) только когда `_initPhase` уже
  не `pending` — то есть всегда. Проверено трассировкой кода, реализуемого
  сценария потери не нашлось; записано как закрытый вопрос, а не как
  опасение — с подавленной для этой ветки базовой парой
  `ScopeWidgetElementBase.reportsOwnLifecycle`, это и есть то, что держит
  `disposed` от потери (задача 2 плана наблюдателя).

### Предмержевое ревью ветки `observer`

Последняя проверка перед мержем: пять находок, все закрыты 2026-08-19. Две из
них были и записями «Открыто, вне списка ревью» — вычеркнуты там же. Что стоит
унести дальше:

- **метка объекта живёт ровно столько, сколько живёт виджет, а хуки бывают
  позже.** `debugLabel` асинхронного элемента читал `widget`, а разбор отдаёт
  виджет в своём `finally` (`_widget = null`, и `Element._widget` к этому
  моменту тоже `null`). Поэтому «брошенное ожидание, упавшее потом»
  (`ScopePhase.abandonedWait`) приходило наблюдателю **как отказ самого
  наблюдателя**: `debugLabel` бросал `TypeError`, защита в `notifyObserver`
  ловила его и репортила про наблюдателя, а настоящий отказ не доходил ни до
  кого. Лечится кешем метки, снятым рядом с `_widget = widget` в `dispose()`.
  Тот же класс граблей уже был записан в контроллерном семействе — там через
  `widget` читали `disposeScopeTimeout`. **Правило: всё, что зовут после
  `_disposalFinished`, не имеет права трогать `widget`;**
- **`ScopePhase.unmount` достижима только через `close()`.**
  `ScopeWidgetElementBase.unmount()` зовёт `unmountScope()` **до** `dispose()`,
  так что у скоупа, снятого с дерева, первая стадия разбора уже пуста, и
  упасть в ней нечему. Тест на эту фазу пишется на `LiteScopeCore` с
  `close()`, а не на `AsyncScope`;
- **отмена в очереди за `scopeKey` не даёт `onInit` вовсе.** Не «`onCancelled`
  раньше `onInit`», как думало ревью, а `onCancelled` в одиночку:
  `_performAsyncInit` возвращается сразу после него, до подписки. Запись
  скоупа целиком — `cancelled`, `dispose`, `disposed`. Записано в теме `debug`
  и в дартдоке `onCancelled`;
- **`RecordingObserver` из `test/utils/observer.dart` теперь оставляет `tag` в
  метке** и срезает только `(#hash)`. Без этого родителя и ребёнка одного типа
  в записи не различить, а тест на ожидание детей без этого не написать;
- **`ScopeAutoDependencies` шлёт `unmount` и `abandonedWait` тоже** —
  `scope_auto_dependency.dart:142` и `:218`. То есть «фаза недостижима» надо
  проверять по всем отправителям, а не только по `async_scope_core.dart`.

### Открыто, вне списка ревью

- ~~**Параллельный `dispose()` одной зависимости зовёт диспозер дважды.**~~
  **Это M1 четвёртого ревью, а не находка вне списка** — M1 называет и
  контейнер, и `scope_dependency_mixin.dart:119`. Оставлено здесь потому, что
  проба ниже — единственное её подтверждение прогоном (у Codex его не было).
  Разбирать вместе с M-блоком; подробности: `_ScopeDependencyImpl._runDispose`
  читает `_helper?.dispose` в начале, а обнуляет `_helper` в `finally`, то
  есть **после** `await`. Второй `dispose()`, пришедший, пока первый ждёт
  диспозера, читает тот же хук и зовёт его повторно: проба даёт
  `log == ['dispose slow', 'dispose slow']`. Последовательный случай закрыт и
  протестирован («a second dispose() does not release anything twice»), это
  именно параллельный. **Почему не починено заодно:** очевидная правка —
  снимать хук до вызова, как это делает `onUnmount` рядом, — делает
  `disposalRequired` ложным на всё время разбора, а его читает
  `_prepareDependencies` контейнера (ветка «дерево доработало — построить
  новое»). То есть правка на одну строку, но с последствием в другом файле, и
  ей нужен свой падающий тест на оба места. Класс тот же, что у H10: факт
  «идёт прямо сейчас» не записан нигде, и все проверки его не видят.
- ~~**«Без ограничения» для одного скоупа не существует.**~~ **Закрыто
  2026-08-20 по решению владельца: `ScopeTimeout.none`, трём таймаутам из
  четырёх.** Отмену инициализации владелец исключил сам — там предел стоит
  ровно от того зависона, который безлимитное ожидание и даёт.

  Форму выбирали пробой, а не рассуждением. Вопрос владельца был: «какой класс
  будет на принимающей стороне, `Object?`» — **нет, `Duration?`**: `Duration`
  расширяется, поэтому маркер `_NoTimeout extends Duration` проходит везде,
  где раньше проходил `Duration`, работает в `const`-конструкторах, и ни один
  параметр не меняет тип. Проба показала главное: `marker is _NoTimeout` —
  `true`, а `Duration(microseconds: -1) is _NoTimeout` — **`false`**, тогда
  как `==` у них `true` (у `Duration` равенство по значению). **Поэтому резолв
  проверяет `is`, а не `==`**: через `==` вычисленный отрицательный `Duration`
  (`deadline.difference(now)`, ушедший в минус) молча стал бы «ждать вечно»
  вместо «истечь немедленно».

  **Грабля реализации:** резолв должен быть **в одной точке**. Сначала скоуп
  резолвил `waitForChildrenTimeout` у себя и передавал результат в
  `waitForChildren`, который резолвил повторно, — и `null`, означавший «без
  предела», превращался обратно в умолчание. Теперь резолвит только
  `AsyncScopeParent.waitForChildren`, а скоуп и
  `AsyncScopeCoordinator.waitForChildren` передают своё значение как есть.

  Ассерт на отмене срабатывает внутри собственной охраны этой стадии разбора,
  поэтому приходит наблюдателю как `onError` с
  `ScopePhase.initializationCancellation`, а не наружу; разбор продолжается.
  Следствие, которое стоит помнить: в debug сработавший ассерт обрывает весь
  блок отмены, то есть отмену не дожидаются вовсе, а в release
  `resolveCancellationTimeout` возвращает умолчание и ожидание идёт как
  обычно. Это поведение неверной конфигурации, не рабочий путь.

  Четыре теста в `test/scope_timeout_test.dart`, все нагруженные: мутация
  резолва валит три, снятый ассерт — четвёртый.
- **Из четырёх ожиданий разбора ограничены три; четвёртое — нет.** Не
  ограничен `await _initCompleter.future` (шаг «инициализацию дожидаются, если
  отменить не удалось»). Это не дефект, а зафиксированная граница: завершают
  этот completer сам разбор сразу после отмены и колбэки подписки, то есть код
  пакета, а не пользователя, и сценария зависания для него не нашлось. Волна 8.
- **`AsyncControllerScopeElementBase` читает приватное `_initSucceeded`**
  соседнего слоя (`AsyncScopeElementBase`). Внутри одной библиотеки законно и
  сделано намеренно: это тот же признак, по которому `_performAsyncDispose`
  решает, звать ли `disposeScope`, и разъехаться они не могут. Связь неявная,
  поэтому закрыта комментарием в коде и мутацией в тестах.
- ~~**`_decideOutside` не спрашивает `onPop`, когда над узлом вообще нет
  навигатора.**~~ **Снято блоком навигации волны 14: ветка недостижима.**
  Диспетчер узла регистрируется на `ModalRoute.of(context)`, и без маршрута
  сверху регистрации нет вовсе, а маршрут без навигатора не существует. То есть
  ранний выход по `_navigator.previous == null` — мёртвый код, а не
  несогласованность; падающий тест на него не напишешь, поэтому строка
  оставлена как есть.
- ~~**`previous.pop()` обходит внешний `PopScope(canPop: false)`.**~~
  **Закрыто блоком навигации волны 14** (`02a4949`), вместе с обоими частными
  случаями находки 3 из
  `docs/records/2026-08-15[9]-navigation-system-back-audit-report.md`. Узел
  теперь **спрашивает** маршрут, на котором стоит (`route.popDisposition`),
  отступив в сторону на длину одного синхронного чтения, а не говорит ему
  `pop`. Единственная страница узла, защищённая своим `PopScope`, удерживается
  ещё раньше — переработкой P1-4. Измерения обоих вариантов и почему
  `previous.maybePop()` не годится (приложение слышит одно нажатие как два) — в
  отчёте `2026-08-17[6]`.
- **Зависимый, перестроенный дважды за один кадр, сохраняет оба набора
  селекторов** — граница сборки взята по кадру, и этот случай она не различает.
  Цена — одна лишняя сборка. Записано в `doc/scope_widget.md`.
- ~~**`Stream.listen(onData:)` пакета зовёт пользовательский логгер, и его отказ
  уйдёт в зону.**~~ **Закрыто 2026-08-19**, и оказалось шире записанного.
  Замер: упавший публикатор не «уходит в зону» из `onData`, а **пробрасывается
  наружу из любого вызова `_log.*`** — тест на простом `AsyncScope` уронил
  сборку (`async_scope_core.dart:523` из `performRebuild`) и следом асинхронный
  разбор (`:861`). То есть опасны были не четыре места с `listen`, а все ~90
  вызовов логгера. Лечится одним хуком: `ScopeConfig.logger` теперь несёт свой
  `onError`, который репортит отказ пути публикации через
  `FlutterError.reportError`, и вызывающий продолжается. Разбор — в «Что
  дальше», п. 6. **С 2026-08-19 логгера нет вовсе** — работа над наблюдателем
  убрала его целиком, а та же защита переехала в `notifyObserver`.
- ~~**`example/scopo_demo/analysis_options.yaml` — копия корневого конфига на
  200 строк.**~~ **Закрыто 2026-08-20**: 285 строк → 20, `include:` корневого
  конфига плюс четыре правила. Проверено правилом за правилом, как и было
  сказано: скрипт разрешает цепочку `include` (`lints/core` →
  `lints/recommended` → `flutter_lints/flutter` → корень → демо) и сравнивает
  действующие наборы до и после — **188 включённых правил и там, и там, ни
  одного расхождения**. Первый заход давал одно расхождение
  (`no_logic_in_create_state` гас, потому что корень выключает его ради своих
  `*Core`-виджетов), поэтому в демо оно включено обратно явным `true` — у демо
  таких виджетов нет. Три выключения проверены мутацией: включённые обратно
  дают 315, 94 и 5 замечаний соответственно, то есть все несущие. Не перенесены
  четыре `exclude` (ни один из этих путей в демо не существует) и три
  severity в `errors:` — `prefer_relative_imports` и `unsafe_html` в корне не
  объявлены вовсе, а `no_logic_in_create_state: warning` косметика:
  `flutter analyze` валится на замечании любой строгости.
- ~~`dart format` расходится на 6 файлах `example/scopo_demo`.~~ **Снято волной
  14 вместе с P2-14 (`7fdbefc`):** пять из шести файлов оказались мёртвым кодом и удалены,
  шестой (`profile_scope.dart`) переформатирован, и `analyze` после этого чист
  — обещанного конфликта с `require_trailing_commas` на нём нет. Витрина даёт
  0 changed на 68 файлах. В гейт §6 форматирование примеров по-прежнему не
  входит.
- ~~**`example/minimal/README.md` и `example/README.md` описывают журнал,
  которого больше нет.**~~ **Закрыто задачей 5**, вместе с зеркалами.
  В `example/README.md` образец кода пересобран из текущего
  `example/minimal/lib/main.dart` целиком (он и был его дословной копией, и
  разошёлся не только журналом: два `const` вне `main()` тоже), русское
  зеркало собрано из того же кода с прежними русскими комментариями. Сверх
  плана задачи 5 туда же попали шесть тем, ссылавшихся на журнал одной фразой:
  `base`, `scope_widget`, `async_scope`, `lite_scope`, `full_scope`,
  `async_controller_scope`.
- ~~**`disposeScope`, упавший при разборе, даёт `onError(disposal)` и не даёт
  `onDisposed`.**~~ **Закрыто предмержевым ревью 2026-08-19 (находка 3).**
  Владелец выбрал первый из двух вариантов и расширил его: **пара
  `onDispose`/`onDisposed` закрывается всегда.** `onDisposed` переехал в
  `finally` третьей стадии — идёт после `onError(disposal)`, а не вместо него,
  — а `onDispose` вынесен из-под `if (_initSucceeded)`, потому что вторая
  половина той же ненадёжности была зеркальной: скоуп, не успевший стать
  готовым, слал `onDisposed` без `onDispose`. «Утилизирован после отказа» —
  это «скоупа больше нет», а не «всё отдано»; так и записано в дартдоке
  `onDisposed` и в теме `debug`.
- ~~**`docs/architecture.md` разошёлся с деревом в двух местах.**~~ **Закрыто
  2026-08-20**, и мест оказалось не два, а восемь. Помимо записанных (таблица
  `ScopeConfig` с `defaultScopeKeysTimeout` и двумя таймаутами из четырёх;
  имена каталогов `a_base`…`h_scope`) разошлись: таблица слоёв и карта
  каталогов не знали слоя `async_controller_scope` вовсе; диаграмма
  наследования показывала четыре класса из девяти; `_shouldOnlyNotify` в §4 —
  имя, которого нет (поля зовутся `_notifyPending` и `_rebuildIsNotifyOnly`);
  `initAsync`/`disposeAsync`/`throwWhenDisposed` — имена до волны 2026-08-18;
  §11 утверждала, что у слоёв нет своих наборов тестов, хотя они есть у всех
  семи. Проверено не глазами: все идентификаторы в обратных кавычках выдернуты
  скриптом и сверены с `lib/`, `test/` и `doc/` — из несовпавших остались
  только `a_base`/`h_scope` в фразе про историю имён, `README.md` и хеш
  коммита в шапке. Гейт §6 этого файла по-прежнему не видит: `docs/` в него не
  входит.
- ~~**`onTimeout` покрывает четыре ограниченных ожидания из шести.**~~
  **Закрыто 2026-08-20 по решению владельца: сведены все шесть.** Добавлены
  `access to its scopeKey` и `its child scopes`. Ожидание детей отчитывается
  из `AsyncScopeParent.waitForChildren` — единственной точки, через которую
  проходят все три способа его запросить (собственный разбор скоупа, прямой
  вызов у родителя, `AsyncScopeCoordinator.waitForChildren`), — и отчитывается
  даже когда вызывающий передал свой `onTimeout`: колбэк заменяет отчёт в
  `FlutterError`, а не то, что пакет сообщает о себе. **Ломающее:** миксин
  `AsyncScopeParent` теперь `implements ScopeObservable` — иначе из миксина до
  наблюдателя не дотянуться (`on Diagnosticable` не даёт `debugLabel`). Все
  элементы пакета этому уже отвечали, `analyze` чист без единой правки на
  местах применения. Два теста, оба нагруженные откатом своей правки; правка
  детей нагружает и существующий тест
  «a failing onWaitForChildrenTimeout reports onError for the preparation
  phase» — там теперь на одно событие больше.

  Ловушки этих тестов, стоившие времени: **дерево с координатором нельзя
  разворачивать целиком** на сценарии «родитель бросил ребёнка» — leak-трекер
  ловит `SingleChildRenderObjectElement`; надо держать `Directionality` и
  менять только скоуп, как делает соседний существующий тест. И **гейт
  `hang.complete()` в теле теста конфликтует с `addTearDown(hang.complete)`** —
  `Bad state: Future already completed`; в `addTearDown` нужен `isCompleted`.
- ~~**провал `init()` структурного семейства невидим наблюдателю.**~~
  **Закрыто 2026-08-20**, и оказалось шире записанного: невидим он был **обоим**
  видам семейств, а не только структурному. Запись выше ошибалась в том, что
  «асинхронная ветка полнее»: полнее она в своей фазе, а синхронный хук
  `init()` — у неё тот же самый, из `ScopeWidgetElementBase`, и он же там
  запускает `widget.onMount(this)`. Фаза стартует в `performRebuild` только
  при `_didInit`, поэтому упавший `init()` оставлял её незапущенной и без
  единого события. Правка одна и стоит в `catch` внутри `build()`:
  `notifyObserver(onError(this, ScopePhase.initialization, …))` перед
  `rethrow`. Один хук — одна точка на все девять семейств. Два теста в
  `scope_observer_test.dart`, оба нагруженные откатом: без правки первый видит
  `[]` вместо ошибки, второй — то же. Попутно уточнён и сам факт: у семейства
  со своей фазой пара `onDispose`/`onDisposed` после упавшего `init()`
  **закрывается** (это и было моим неверным ожиданием в тесте), у структурного
  — не открывается вовсе.
- ~~**отказ `_releaseController` невидим наблюдателю.**~~ **Закрыто
  2026-08-20.** `notifyObserver(onError(this, ScopePhase.disposal, …))` перед
  прежним `FlutterError.reportError`, ровно как это делает обычный разбор в
  `_performAsyncDispose`. Тест в `scope_observer_test.dart` (контроллер,
  у которого падает и `init`, и `dispose`), нагруженный откатом.
- ~~**второй из двух одновременных отказов разбора `ScopeCore` невидим
  наблюдателю.**~~ **Закрыто 2026-08-20, и находка снова оказалась шире
  записанного.** Падающий тест показал, что наблюдатель не получал **ни
  одного** из двух отказов `onUnmount`, а не только второй. Причина у первого
  своя и лежит уровнем выше: `ScopeWidgetElementBase.unmount()` звал
  `unmountScope()` без `notifyObserver` вовсе. Тот же хук защищает и первая
  стадия `_performAsyncDispose` — она шлёт `onError(unmount)`, — но до
  `unmountScope()` она добирается первой только на пути `close()`, где элемент
  остаётся на дереве; когда дерево забирает элемент, первым успевает
  `unmount()`, а `_didUnmount` делает вторую попытку пустой. То есть один и
  тот же хук на двух путях отчитывался по-разному, и существующий тест
  «a failing onUnmount reports onError for the unmount phase» покрывал только
  путь `close()`. Правок две: `catch`/`rethrow` вокруг `unmountScope()` в
  `unmount()` и `notifyObserver` рядом с обоими `_reportFailure` в
  `scope_core.dart`. Два теста в `cleanup_after_user_error_test.dart`,
  нагруженность проверена откатом каждой правки по отдельности. Заодно снято
  подозрение с четырёх стадий `_performAsyncDispose`: там `take()` вызывается
  **после** `notifyObserver(onError(...))` на каждой стадии, так что
  `_reportFailure` оттуда — второй канал для отказа, о котором наблюдатель уже
  услышал.
- ~~**отказ пользовательского `dispose` у `ScopeModel` невидим
  наблюдателю.**~~ **Закрыто 2026-08-20.** Вызов `dispose()` в `unmount()`
  обёрнут в `_disposeReportingFailure()`: `onError(…, ScopePhase.disposal, …)`
  и `rethrow`. Тест в `scope_observer_test.dart`, нагруженный откатом.
  Особенность теста, которую стоит помнить: **дерево нельзя менять целиком**
  (`pumpWidget(const SizedBox())`) — отказ вылетает из `Element.unmount()`,
  обход фреймворка на этом кончается, и leak-трекер ловит неутилизированный
  `_UbiquitousInheritedElement`. Забирать надо только сам скоуп, оставив
  `Directionality` над ним. Это свойство падающего разбора, а не скоупа.
- ~~**отказ `buildOn*` до наблюдателя не доходит — и это граница, а не
  пробел.**~~ **Закрыто 2026-08-22 решением владельца** (`e7b01c7`, дизайн —
  `docs/records/2026-08-22[1]-build-phase-design.md`), и «отдельное решение»
  оказалось единственной трудной частью: сама правка — семь строк в одном
  файле. Что стоит унести дальше:

  - **точка нашлась одна на все девять семейств, и не потому, что повезло.**
    `ScopeWidgetElementBase.build()` помечен `@nonVirtual`, а у `buildChild()`
    во всём пакете один вызов — эта строка. Ниже неё расходятся четыре
    реализации, три из которых зовут пользовательский код: `buildOnState`
    асинхронных семейств (пять `buildOn*`), `ScopeWidgetBase.build` и
    `ScopeModel.build`. Прежде чем ставить охрану в каждое семейство, стоит
    посмотреть, нет ли у них общего предка, который уже `@nonVirtual`;
  - **правило «отказ без адресата отчитывается и не бросается» здесь работает
    наоборот.** У `unmount()` элемента адресата нет, поэтому там отчёт заменил
    бросок. У сборки адресат есть и он несущий — граница ошибок Flutter,
    рисующая `ErrorWidget`. Значит отчёт **добавляется** к броску: видимое
    поведение не меняется ни на кадр;
  - **второй тест держит не покрытие, а выбор места охраны.** Проверено
    мутацией: перенос охраны в `buildOnState` (то есть «узкий» вариант, ровно
    по букве этой записи) оставляет первый тест зелёным и роняет второй. Без
    второго теста узкая правка выглядела бы закрывшей находку;
  - **`comment_references` ловит `[Имя]` из чужого слоя.** `[ScopeWidgetBase]`
    и `[ScopeModel]` в дартдоке `lib/src/environment/` не резолвятся — два
    замечания корневого `analyze`. В `environment/` ссылаться на слой скоупа
    надо обратными кавычками;
  - **поиск по соседям сошёлся, и это конец списка.** Билдеры `NavigationNode`,
    `ListenableSelector` и представлений `ScopeNotifier` под фазу не попадают,
    и попасть не могут: `onError` требует `ScopeObservable`, то есть
    `debugLabel`, а ни один из трёх скоупом не является. Это граница по
    построению, а не следующая находка.

### Тулчейн и окружение

- **CI пишет предупреждение про Node.js 20 на каждом прогоне.**
  `actions/checkout@v4` (`.github/workflows/ci.yml:25`) заявляет Node 20,
  раннер принудительно даёт ему Node 24 и сообщает об этом аннотацией. Прогон
  зелёный, это не отказ. Лечится подъёмом до `actions/checkout@v5`. Не сделано
  намеренно: замечено 2026-08-22 по ходу другой работы, а чинить смежное заодно
  здесь не принято (`AGENTS.md` §8). `subosito/flutter-action@v2` рядом не
  жалуется.
- **Пол Flutter держал `logger_builder`, а не код scopo** — запись верна для
  0.5.0 и отменена ниже: 2026-08-18 пол опущен до 3.27.0. Сам пакет прекрасно
  жил бы на Flutter 3.27, о чём здесь и было сказано. Мешал опубликованный
  `logger_builder 0.5.0`: он объявляет `sdk: ^3.2.0`, но требует `meta ^1.16.0`,
  а Flutter пинит `meta` **точной** версией (`meta: 1.16.0` в
  `packages/flutter/pubspec.yaml` и `packages/flutter_test/pubspec.yaml` у
  3.29.0; у 3.27 записано `1.15.0` — цифра из этой записи, на живом 3.27 не
  перепроверялась, его в fvm нет). Сам `meta` ни при чём: и 1.15.0, и 1.16.0
  объявляют `sdk: ">=2.12.0 <4.0.0"`.

  **Это положение изменилось 2026-08-17, и запись про «нельзя» устарела:
  владелец опубликовал `logger_builder` 0.6.0 и 0.6.1 в тот же день**, и обе
  объявляют `sdk: ^3.6.0` с `meta: ^1.15.0` — то есть именно то ослабление,
  которого не хватало. Проверено по pub.dev API, не по локальному дереву:

  | версия | опубликована | `sdk` | `meta` |
  | --- | --- | --- | --- |
  | 0.5.0 | 2026-07-30 | `^3.2.0` | `^1.16.0` |
  | 0.6.0 | 2026-08-17 | `^3.6.0` | `^1.15.0` |
  | 0.6.1 | 2026-08-17 | `^3.6.0` | `^1.15.0` |

  Следствия:

  - ~~пункт бэклога «обновить `logger_builder`»~~ — **сделан 2026-08-18**:
    в `pubspec.yaml` стоит `^0.6.1`, локи трёх примеров пересозданы закреплённым
    тулчейном, гейт зелёный целиком. Кода менять не пришлось: поверхность, которой
    пользуется scopo, та же. Проверено отдельно, что ломающие правки 0.6.0 сюда
    не бьют: уровни регистрируются четырьмя (`v`, `d`, `i`, `e`), ни одного на
    порогах `all`/`off`, а `off` используется ровно как порог
    (`ScopeConfig.logger.level`);
  - **сцепка с подъёмом `sdk` до `^3.7.0» распалась**: 0.6.1 объявляет `^3.6.0`,
    так что обновление зависимости больше не тянет за собой ни tall style, ни
    снятие `require_trailing_commas`. Прежняя запись в «Что дальше» говорила
    обратное — она была верна для локальной 0.6.0, у которой в тот момент был
    заявлен `^3.7.0`, и pub.dev это не подтвердил;
  - **пол опущен до 3.27.0 — 2026-08-18, `0b39347` и `a70a550`.** Внешнего
    ограничения не осталось (`meta ^1.15.0` сходится и с 3.27), а решение принял
    владелец — по замеру, а не по рассуждению: HEAD склонировали в скретч
    сессии, поставили `fvm install 3.27.0` (Dart 3.6.0) и прогнали гейт там, не
    трогая рабочее дерево. На 3.27.0 прошли все восемь команд §6 — 380 тестов,
    `analyze` чист в корне и во всех трёх примерах, `format` 0 changed на 95
    файлах, `dart doc` 0/0, `publish --dry-run` 0 warnings на чистом дереве,
    переводы 16 — и обе сьюты примеров. Заплачено ровно то, что показал замер:
    - `fake_async ^1.3.2` → `^1.3.1` и `leak_tracker_flutter_testing ^3.0.9` →
      `^3.0.8`: Flutter пинит и то и другое **точными** версиями, у 3.27 это
      1.3.1 и 3.0.8. Обе — dev-зависимости, потребителя не касаются, и на 3.29
      те же ограничения по-прежнему дают 1.3.2 и 3.0.9;
    - `ansi_escape_codes ^3.1.2` → `^4.0.1`: та же ловушка `meta`, что была у
      `logger_builder`, — 3.1.2 требует `meta ^1.16.0`, а 3.27 пинит 1.15.0;
      владелец снял её в 4.0.1. Кода правка не задела: `test/utils/logging.dart`
      берёт `Printer`, `Style`, `Color` и `Color256`, все на месте. В
      `example/minimal` и `example/scopo_demo` это уже не dev-, а обычная
      зависимость;
    - `example/scopo_demo` — единственная настоящая работа. Длинное нажатие
      висит теперь на `GestureDetector`, потому что `IconButton.onLongPress`
      появился только в 3.29, а подсказка поднята над ним отдельным `Tooltip`:
      `IconButton.tooltip` кладёт свой `Tooltip` **под** обёртку, и жест
      забирал он (см. «Грабли»). Порядок держит новый тест
      `example/scopo_demo/test/home_app_bar_test.dart`.
      `buildOnReady(..., CounterModel value)` стал `data`, как в базе, а
      `buildDependencies(_)` — `buildDependencies(void context)`: wildcard
      пришёл с Dart 3.7, а голое имя без типа ловит `type_annotate_public_apis`
      уже на 3.29. **Оба линта бьют только по публичным классам** — те же `(_)`
      в приватных классах `test/scope_auto_dependencies_test.dart` молчат на
      обоих полах, поэтому в `test/` править было нечего;
    - не код: `.fvmrc`, §6 `AGENTS.md`, `flutter-version` в CI, локи трёх
      примеров и три записи в `CHANGELOG.md` — ранние, поднимавшие пол, теперь
      прямо говорят, чем их отменило. 3.27.0 стоит в fvm рядом с 3.29.0;
  - **`sdk: ^3.7.0` отменён владельцем тем же решением.** Пол 3.27 и Dart 3.7
    вместе не живут: 3.7 приходит только с Flutter 3.29. Выбрана сторона пола —
    значит `sdk: ^3.6.0` остаётся и впервые становится достижимым, а tall style,
    снятие `require_trailing_commas` и реформат 69 файлов сняты с повестки
    вместе с записью бэклога. Этим же закрыт последний хвост P3-21 — вердикт
    дополнен в самом ревью;
  - **с 0.6.1 у логгера появился `CustomLogger.onError`** — один хук на все
    отказы пути публикации. Это готовый ответ на смежную проблему, записанную
    ниже в «Открыто, вне списка ревью»: пользовательский логгер, который бросает
    из `_log.d(path)` внутри `Stream.listen(onData:)`, уводит отказ в зону.
    Не сделано: это отдельная работа и, возможно, кусок публичного API
    (`ScopeConfig`), а не часть обновления зависимости.
- **`sdk: ^3.6.0` совпадает с полом ровно, и это проверяется каждым прогоном.**
  Flutter 3.27.0 несёт Dart 3.6.0 — ровно нижнюю границу `sdk`. До 2026-08-18
  граница не связывала ничего (пол 3.29.0 нёс Dart 3.7.0) и держалась ради
  **языковой версии**: на 3.6 `dart format` работает в старом стиле, на 3.7
  переключается на tall style. Измерено 2026-08-17 (поднять `sdk`, `pub get`,
  `dart format -o none --set-exit-if-changed`): **69 файлов из 94 переформатируются**.
  Форматтер берёт языковую версию из `.dart_tool/package_config.json`, поэтому
  проверять это без `pub get` бессмысленно — первая проба показала 0 changed и
  была неверной.

  **Подъём до `^3.7.0` отменён владельцем 2026-08-18** и удалён из бэклога.
  Тянул за собой снятие `require_trailing_commas`
  (`analysis_options.yaml:219`): tall style *убирает* запятые, которых требует
  правило. Вернётся он только вместе с полом 3.29. В `logger_builder` это уже
  прожито — 37 файлов из 45 и 43 замечания анализатора на дереве, которое
  `dart format` только что создал (`6128bc8`). Вероятно, той же правкой
  растворится известная проблема с шестью файлами `example/scopo_demo` ниже:
  там расхождение ровно с этим правилом.
- Расхождения между версиями анализатора реальны: два замечания `analyze`,
  закрытые в `b697775`, видел только тулчейн 3.29, но не 3.44.9. Отсюда правило
  работать на поле.
- Flutter 3.29.0 с Xcode 26.5 печатает warning о двух совпавших macOS
  destinations. Косметика старого Flutter tools, release всё равно universal;
  upstream исправил в 3.35.0. Не подавлять. Разбор:
  `docs/records/2026-08-14[8]-xcode-destination-warning-report.md`.
- `flutter run -d macos --release` не завершается следом за закрытым
  приложением — открытый баг Flutter tools `flutter/flutter#58459`, не утечка
  scopo. Для проверки release собирать `fvm flutter build macos --release` и
  открывать `.app` напрямую. Разбор:
  `docs/records/2026-08-14[7]-release-run-hang-report.md`.

### В рабочем дереве

- дерево чистое. `docs/backlog.md` пуст и совпадает с `HEAD`: запись владельца
  от 2026-08-19 («стоит ли дальше использовать logger_builder») лежала
  незакоммиченной, работа по ней сделана, и удаление по §3 вернуло файл ровно
  к тому, что уже было в истории, — поэтому в диффе задачи 5 его нет вовсе;
- каталоги `.claude/` и `.superpowers/` — чужие, рабочие каталоги агентов. В
  `git status` они не показываются (исключены не через `.gitignore`, а глобально
  или в `.git/info/exclude`), но лежат в корне: не трогать и не добавлять в
  индекс;
- в `git stash` лежит чужой давний стеш `90ccbbc update README` — не трогать;
- корневой `pubspec.lock` в `.gitignore`; локи трёх примеров закоммичены и
  пересозданы закреплённым тулчейном.

## Пробелы в покрытии

**Сьюта измерена мутациями 2026-08-20: эффективный score ≈ 83 %** — 94
прицельные мутации, 70 пойманы, из 24 выживших около семи эквивалентны. Падает
она точно: обычно 1–3 теста, и имя теста дословно называет сломанный инвариант.
Полностью нагружены освобождение `scopeKey` (21 тест на одну мутацию),
регистрация у родителя (12), однократность `unmountScope` (18),
`_closeCompleter` (26), таймер `Zone.root` (16).

**Пять названных пробелов закрыты волнами 18–25, кроме одного, который закрыть
нельзя** (разбор и мутации — в четвёртом ревью, находки H12, M11–M18; вердикт
у каждой на месте). Ниже — как было записано, с отметкой, чем закрыто:

1. ~~Скоуп сняли с дерева, пока `initStateAsync` ещё висит~~ — **закрыто
   волной 18**: третья группа в `scope_removed_while_initializing_test.dart`,
   два теста (H12).
2. ~~Поток инициализации, живущий после ошибки~~ — **закрыто волной 25**:
   группа `'AsyncScope initialization driven from a StreamController'` (M12).
   Вторая из двух взаимно-избыточных охран тестом не закрывается по построению
   — причина написана над ней в коде.
3. ~~Перестройка от предка после уведомления~~ — **закрыто волной 21**:
   `'a notification is spent by the rebuild that acts on it'` (M13).
4. ~~Композиция `isBuilding`~~ — **закрыто волной 21**: каждое слагаемое
   изолировано состоянием, в котором два других ложны (M11). Перепроверено
   2026-08-22 мутацией всего геттера: из семи тестов файла шесть роняются
   `=> true` или `=> false`, седьмой — мутацией `scopeIsRebuilding`, то есть
   нагружены все.
5. Release-ветки, отделённые от debug ассертом (M5, M18), — не проверяются
   принципиально, потому что `flutter test` всегда с ассертами.

~~**Три теста, которые не могут упасть**~~ — **все разобраны, 2026-08-22**.
Два в `is_building_test.dart` перестали быть тавтологией волной 21 (см. пункт 4
выше). «gives up after the retry cap» в `lite_scope_test.dart` считает кадры до
потолка — волна 19, L12. Третий был не про тест, а про код: ветка
`total == 0 ? 1` в `Progress.value` мертва, потому что `clamp` сравнивает через
`compareTo`, а тот считает `NaN` максимальным `double`, — то есть обе формы
дают `1.0` и различить их нечем. Ветка убрана, причина записана в дартдоке;
теперь снятие `clamp` роняет два теста.

**Мигающих тестов не нашлось.** Дисциплина с реальным временем выдержана.

**`notifyDependents` и `setState` — две половины, и это закреплено тестом**
(`lite_scope_test.dart`, группа «notifyDependents and setState are separate
halves», 2026-08-20). Владелец спросил, сказано ли в `README.md`, что
`setState` в скоупе «отключен». Не сказано — и было бы неверно: слова
`setState` нет в `lib/src/scope/` вовсе, состояние скоупа — обычный `State`.
Проба показала расклад: `notifyDependents()` доходит до подписчиков и
собственный `build` состояния **не** выполняет, `setState` — наоборот. Обе
половины теперь названы в `README.md` таблицей, а ловушка («поменял поле,
позвал только `notifyDependents()` — свой `build` не выполнится») названа
прямо. Тесты нагруженные: мутация `_rebuildIsNotifyOnly = false` валит первый,
добавленный `notifyDependents()` в `bumpWithSetState` — второй.

Темы поправлены следом, по просьбе владельца: в `doc/lite_scope.md` аналогия
(«тот же контракт, что у `StatefulWidget` с `setState`») заменена на прямое
изложение обеих половин, и по правилу «найди то же у соседа» то же дописано в
`doc/full_scope.md` — состояние `Scope` описывалось так же односторонне.
Попутно в `lite_scope.md` исправлено «four additions» над таблицей из пяти
строк (и «четырьмя добавками» в зеркале). `doc/scope_model.md:137` проверен и
оставлен: там `setState` у `StatefulWidget` **над** скоупом, случай другой и
описан верно. `doc/scope_widget.md` тоже оставлен: у `ScopeWidget` состояния
нет, и `setState` там ни при чём.

Раздел ведётся по факту. Прежняя формулировка «крупных пробелов не осталось,
покрыты все слои» была неверна и снята после повторного ревью. Волны 10–13
закрыли весь известный на тот момент список — **и третье ревью открыло новый**.

**Волна 15 добавила 17 тестов** (363 → 380), и почти каждый — из тех, что
пишут не для покрытия, а чтобы решить спор: два на отказ guard'а в узле
навигации, два на тождество списка страниц и на счётчик уведомлений, один на
восстановление `ListenableSelector` после упавшего селектора, два на
`ScreenshotReplacer.onCompleted`, один на имя в истёкшем ожидании детей, один на
чтения `scopeKey` при отмене очереди, один на диагностику без несуществующей
очереди, один на «сообщение всегда есть», один на «группа хранит одну ошибку
братьев», два на подстановку `_NullWidget`, один на окно между `yield` и его
следующей строкой и один на отменённый разбор, который ведут сами. Что осталось
непокрытым сознательно: `reportName` у чужого родителя, статика
`AsyncControllerScopeCore` (те же `ScopeContext.*`, покрытые у соседних слоёв),
приватный конструктор `NodeNavigatorState` и охрана кадра в
`_currentBuildPass()` — у последней нет достижимого сценария, и это записано в
вердикте P3-4.

**Список третьего ревью закрыт волной 14** (P2-13, тестов 323 → 363). Из 15
незамеченных мутаций закрыты 14; пятнадцатая — охрана `!_isDisposing` в
таймере паузы — **недостижима**, и это проверено пробой, а не рассуждением:
разбор ставит флаг и следующим оператором гасит таймер, завести новый может
только подписка, которую тот же разбор отменяет до первого `await`. Причина
записана комментарием на месте.

Что появилось: `test/scope_parameters_matrix_test.dart` — 32 теста на все 55
пар «семейство × параметр»; поисковая поверхность `Scope` в
`public_facades_test.dart`; ветка «безлимитное ожидание» в
`scope_config_test.dart`; `CompareUtils`, `runStreamGuarded` с падающей
фабрикой и цена упавшего селектора для его собственной подписки.

Покрыто сильно: освобождение `scopeKey`
(`test/async_scope_coordinator_test.dart`), порядок разбора на обоих путях
(`test/scope_lifecycle_order_test.dart`), `close()` во всех четырёх состояниях
и `ScreenshotReplacer` прямыми виджет-тестами (`test/lite_scope_test.dart`),
дерево из десяти зависимостей с четырнадцатью комбинациями падений
(`test/scope_auto_dependencies_test.dart`), `throwIfDisposed`
(`test/listenable_utils_test.dart`), пять путей разбора контроллера
(`test/async_controller_scope_test.dart`).

**Закрыто волнами 10–11** (11 новых тестов, всего 242):

- поверхность параметров обеих замыкательных форм со стороны конструктора —
  `test/async_scope_parameters_test.dart` (было главным пробелом: вся сьюта
  ходила к этим настройкам через рукописные подклассы);
- синхронный провал инициализации показывает ветку ошибки
  (`async_scope_test.dart`);
- упавший `ScopeState.onUnmount` не пропускает unmount зависимостей
  (`cleanup_after_user_error_test.dart`);
- падающий диспозер в `concurrent`-группе, стектрейс ошибки зависимости и
  репорт её разбора (`scope_dependency_partial_test.dart`);
- упавший селектор не съедает уведомление; уведомление не строит поддерево
  (`scope_widget_test.dart`);
- первая ошибка разбора, `close()` состояния после ухода дерева, диагностика
  `of()` до готовности (`lite_scope_test.dart`);
- композит подписок не обрывается (`listenable_utils_test.dart`);
- падающий и зависший `dispose()` контроллера
  (`async_controller_scope_test.dart`);
- дефолт таймаута у миксина `waitForChildren`
  (`async_scope_coordinator_test.dart`);
- подписка вне сборки отвергается (`base_test.dart`).

**Закрыто волной 12** (22 новых теста, всего 264):

- границы `Progress` и ассерты `ProgressIterator`
  (`progress_iterator_test.dart`);
- `isBuilding` и `runOutsideFrame` — четыре теста в новом
  `is_building_test.dart`, включая сборку вне кадра;
- `runStreamGuarded` получил первый собственный файл —
  `run_stream_guarded_test.dart`: счётчик суб-логгеров и имя вызывающего в
  сообщении;
- `StateAsNotifier` после ухода состояния (`listenable_utils_test.dart`);
- упавший асинхронный `onPop` (`navigation_node_test.dart`);
- односторонность и совместный разбор у `ScopeController`, три теста
  (`async_controller_scope_test.dart`);
- `ScopeConfig.reset()` — новый `scope_config_test.dart`;
- второй `ready` не подменяет значение; `hasData` для nullable `T`
  (`async_data_scope_test.dart`);
- модель скоупа — один объект (`async_scope_test.dart`);
- `no disposal required` у листа, которому нечего отдавать
  (`scope_auto_dependencies_test.dart`, плюс девятнадцать строк снимков);
- поиск без скоупа запоминается как зависимость (`base_test.dart`);
- состояние скоупа отказывает в `widget` (`lite_scope_test.dart`).

**Закрыто волной 13** (29 новых тестов, всего 293) — весь список того, что
оставалось непокрытым, разбор в `2026-08-17[2]-coverage-gaps-report.md`:

- дерево зависимостей, отменённое на полпути **через виджет**, и то же обещание
  слоем выше — защита написанной руками инициализации
  (`scope_removed_while_initializing_test.dart`, новый файл, семь тестов);
- падение `onUnmount` и `dispose()` контроллера **после** готовности, плюс
  брошенное ожидание, упавшее позже (`cleanup_after_user_error_test.dart`);
- публичные фасады: `LiteScope`, `LiteScopeState`, `ScopeWidgetBase`
  (`public_facades_test.dart`, новый), `ScopeStateNotifier`,
  `ScopeStateWithErrorNotifier` и их представления (`scope_state_model_test.dart`,
  новый);
- затенение скоупа того же типа вложенным (`base_test.dart`);
- фильтрация по уровню логирования, включая наследование порога суб-логгером
  (`scope_logger_test.dart`);
- связка `isRoot` + `onPop`, обе ветки решения (`navigation_node_test.dart`).

**Закрыто волной 14** (363 теста) — не по списку ревью, а по ходу закрытия
находок:

- **обещание `unmount` у зависимостей на провальном пути** —
  `scope_dependency_unmount_test.dart`, новый файл, четыре теста: порядок
  относительно `dispose` на обоих путях и однократность в двух видах;
- **гарантия `onUnmount` самого скоупа при провале инициализации** — её не
  было **вовсе**, и мутация это показала: `unmountScope()` закрывался условием,
  а сьюта оставалась зелёной. Закрыта двумя тестами, по одному на путь —
  снятие с дерева (`async_scope_test.dart`) и `close()`
  (`lite_scope_test.dart`);
- **уведомление изнутри собственной сборки** — два теста в
  `scope_notifier_test.dart`;
- **порядок разбора на двух провальных путях** — два теста в
  `scope_lifecycle_order_test.dart`; готовый путь там был закреплён и раньше.
  Эти два защищают таблицу из темы `Scope`;
- **весь прогон идёт под leak-трекером**, см. шаг 0 волны 14;
- **узел на первом маршруте приложения** — случай, куда ни один тест
  `NavigationNode` раньше не заглядывал: все пушат узел вторым маршрутом
  (`navigation_node_test.dart`, блок навигации);
- **`Drawer` внутри узла и тот же экран без узла** — два теста подряд, второй
  контрольный: узел не должен отбирать то, что без него работает;
- **стрелка `AppBar` на первой странице узла** — вид и нажатие, плюс её
  отсутствие у корневого узла. Вид был покрыт только сьютой примера, а она в
  гейт §6 не входит;
- **забытый билдер `LiteScope`** — два теста в `public_facades_test.dart`:
  переопределён `initScope()`, а `buildOnProgress`/`buildOnError` оставлены
  дефолтными;
- **диалог `onPop` принадлежит узлу** — читает скоуп, под которым стоит узел;
  тот же тест переписан и в `example/navigation_node`;
- **два отказа одного разбора** — три теста: вторая стадия разбора
  `AsyncScope` (пара «истёкшее ожидание детей + упавший `disposeScope`») и обе
  половины разбора `Scope` на рукописном контейнере
  (`cleanup_after_user_error_test.dart`);
- **release-видимая половина `isBuilding`** — сборка скоупа вне кадра
  (`is_building_test.dart`). Тест читает счётчик напрямую: в debug ответ даёт
  ассертный флаг, и тест на сам `isBuilding` прошёл бы и без правки.

**Список непокрытого от третьего ревью закрыт** — см. начало раздела.

Тесты-пустышки, не трогали: `test/async_scope_state_test.dart` целиком (оба
теста считают скобки и пройдут при `toString() => ''`), тавтологический ассерт
в `test/scope_logger_test.dart` (`expect(drop, isA<ScopeLogTransformer>())` —
проверяет тип литерала, который сам же и объявлен строкой выше). Волна 13 этот
файл правила, но ассерт не трогала: судьба пустышек — решение владельца. Это не
пронумерованные находки — они из
раздела ревью о тестах (`2026-08-16[12]`, строки 1046–1050), поэтому вердикта
у них нет и в счёт 43 они не входят.

**Третье ревью нашло ещё три пустышки, и они уже в счёт входят** — как часть
P2-13: тавтология `indexWhere(...) < indexWhere(...)` в
`example/scopo_demo/test/async_controller_example3_test.dart:35-39` (проходит,
если `onUnmount` не залогирован вовсе, то есть при той самой регрессии, ради
которой написана), шесть тестов в цикле
`example/navigation_node/test/lessons_test.dart:66-77` (утверждают, что
корневой виджет всё ещё на месте — он не может исчезнуть) и
`isA<StateError>()` дважды вместо `same()` в
`test/async_controller_scope_test.dart:89-113`, где сам тест называется «every
caller of performDispose sees **the same** failure».

Что осознанно не покрыто и покрыто быть не может — гарантии анализатора:
закрытость `State.dispose`, закрытые обёртки `ScopeController`, запечатанные
волной 11 `initScope`/`initDataAsync`/`disposeScope`, и не-nullable `value` у
`.value`-конструкторов (там гарантия компилятора). Сюда же — снятая волной 12
аннотация `@visibleForTesting` с `LiteScopeCoreState.widget`: сам отказ теперь
проверяется тестом, а вот то, что геттер запечатан, — нет.

## Что дальше

**Списки всех четырёх ревью закрыты целиком: 18 + 43 + 44 + 52.** Третье
закрыто волнами 14 (семь P1, шестнадцать P2) и 15 (двадцать одна P3),
четвёртое — волнами 16–25. У каждой находки в самом ревью стоит вердикт. Что и
чем закрыто, таблицами — в разделах «Волна 15» и «Волна 14», по волнам 16–25 —
в пунктах 10–13 ниже; что закрыто не так, как предлагало ревью, — в
«Известные проблемы» и в самих вердиктах.

**Очередь работ пуста.** Ниже — список того, что уже сделано: пункты
вычеркнуты и оставлены, чтобы было видно, чем именно закрыты. Пункт 5 —
публикация: единственное, что ещё не решено, и решает это владелец. Пункт 9 —
работы 2026-08-20 не из ревью, а из раздела «Открыто, вне списка ревью»;
пункты 10–13 — волны по четвёртому ревью.

1. ~~**Переименования из бэклога.**~~ **Сделано 2026-08-18, бэклог пуст.**
   Аудит — `docs/records/2026-08-18[1]-naming-review.md` (20 находок, у каждой
   вердикт), решения и порядок — `docs/records/2026-08-18[2]-naming-plan.md`.
   Пять волн: `2c16390` (внутренние имена), `18f3739` (семь мелких публичных),
   `0124289` (`progressBuilder`/`buildOnProgress`), `be530b2` (жизненный цикл:
   `initScope`/`disposeScope`, `initStateAsync`/`disposeStateAsync`, таймауты,
   параметры удобных виджетов), `dca3924` (`ScopeDependency`,
   `AsyncControllerScopeContext`, `shouldNotify`, дартдок про progress).
   Каждая волна — с полным гейтом §6 на 3.27.0. Две находки не подтвердились:
   `paramsOf`/`selectParam` и тройка `ScopeController`.

   **Правило имён, которое владелец сформулировал по ходу и которое стоит
   помнить:** голый глагол значит, что метод делает названное (`initScope`
   инициализирует, `disposeScope` освобождает), а `on…` — что метод вызывается,
   когда это случилось, но сам этого не делает (`onUnmount` не размонтирует).
   По нему сняты две находки и названы все новые имена. В `AGENTS.md` оно не
   записано — если владелец захочет сделать его частью регламента, место ему в
   §7. Делалось это сейчас потому, что 0.10.0 ещё не опубликована: в релизе и
   так есть ломающие правки (P1-5, закрытый конструктор `NodeNavigatorState`),
   так что ломающая волна имён не добавила консьюмеру ни одного лишнего
   перехода. После публикации то же самое стоило бы мажорной версии.
2. ~~**Обновить `logger_builder`.**~~ **Сделано 2026-08-18** (`^0.6.1`): кода
   менять не пришлось, гейт зелёный, локи примеров пересозданы. Пункт вычеркнут
   из бэклога. Разбор — в «Тулчейн и окружение».
3. ~~**Поднять `sdk` до `^3.7.0`.**~~ **Отменено владельцем 2026-08-18.** Пол
   ушёл на 3.27.0, а Dart 3.7 приходит только с Flutter 3.29 — вместе они не
   живут. Запись удалена из бэклога; этим же закрыт последний хвост P3-21, и
   реформата 69 файлов не будет.
4. ~~**Опустить пол Flutter.**~~ **Сделано 2026-08-18** — `0b39347`
   (зависимости и демо) и `a70a550` (пол, `.fvmrc`, CI, регламент). В
   `pubspec.yaml` `>=3.27.0`, гейт пройден на 3.27.0 целиком, CI пинит тот же
   номер. Полный разбор — в «Тулчейн и окружение». **Тулчейн сессии сменился:
   `fvm flutter` — это теперь 3.27.0 / Dart 3.6.0.**
5. **Публикация 0.10.0** — по явной просьбе владельца, не раньше; на 2026-08-17
   он сказал, что не торопится. **Ничто её больше не блокирует:** рекомендация
   третьего ревью («не публиковать до закрытия P1 и P2») выполнена, вердикт
   четвёртого («отложить до разбора P0 и P1») выполнен с запасом — владелец
   решил чинить всё, и все 52 находки закрыты.

   Что уедет в релизе ломающего, если публиковать: пара
   `onDispose`/`onDisposed`, `AsyncScopeParent implements ScopeObservable`,
   запрет `ScopeTimeout.none` на отмене и на паузе, запечатывание
   `ScreenshotReplacer` и `ListenableSelector`, `show` вместо `hide` в бареле,
   ассерт вместо броска в `CompositeListenableSubscription.add` и новое
   значение `ScopePhase.build`. Всё это сделано **до** публикации намеренно:
   после неё каждый пункт стоил бы мажорной версии.
6. ~~**`CustomLogger.onError` из `logger_builder` 0.6.1.**~~ **Сделано
   2026-08-19.** `ScopeConfig.logger` ставит свой `onError`
   (`scope_config.dart`, `_reportLoggerFailure`): отказ пути публикации —
   упавший публикатор, упавший трансформер, срабатывание защиты от рекурсии —
   репортится через `FlutterError.reportError` с `library: 'scopo'`, и
   вызывающий продолжается. Публичного API не прибавилось: хук достижим как
   `ScopeConfig.logger.onError`, свой обработчик его заменяет, `null` возвращает
   прежнее поведение (отказ наружу из вызова журналирования), `ScopeConfig.reset()`
   его не трогает. Три теста в `scope_logger_test.dart`, из них один прогоняет
   скоуп целиком; первый падал до правки на `threw StateError: publisher
   failed`. Новый раздел в `doc/debug.md` с зеркалом.
7. ~~**Четвёртое ревью** — если владелец захочет.~~ **Проведено 2026-08-20 по
   просьбе владельца:** `docs/records/2026-08-20[1]-project-review.md`. Семь
   независимых ревьюеров — шесть сабагентов по областям плюс Codex. **Одна
   Critical, четырнадцать High, восемнадцать Medium, девятнадцать Low.**
   Публикация 0.10.0 на паузе до разбора C1 и блока High. Ни одна находка не
   закрыта — списка работ ещё нет, есть список находок.

   **Три вещи из него, которые определяют, что делать дальше.** Первая: C1 —
   в коде, написанном в тот же день. `ScopeTimeout.none` снимается резолвером
   с параметра скоупа, но не с глобального умолчания, а маркер — подтип
   `Duration`, поэтому проходит в любой слот, где ждут `Duration`. Отсюда же
   H8, M7 и M8: это не четыре ошибки, а одно недодуманное следствие формы,
   которую я предложил. Вторая: **один класс дефекта в пяти местах** —
   «пользовательский хук бросил, обязательная уборка не выполняется». Сессия
   2026-08-20 закрыла четыре случая того же класса со стороны *видимости*
   отказа наблюдателю, а вопрос *продолжения уборки* остался открытым.
   Третья: сьюта сильная (**mutation score ≈ 83 %**, 70 пойманных мутаций из
   94), но три теста не могут упасть, и найдены пять непокрытых мест с
   названным риском.

   **Ошибка организации, которую не надо повторять:** мутационный ревьюер
   работал в общем дереве параллельно с остальными и испортил им прогоны —
   трое ловили чужие правки в `git status`, одному снесли каталог в scratchpad,
   чистого baseline он предъявить не смог. Параллельные ревью надо разводить
   по worktree. Codex вдобавок не смог запустить ни одного теста: песочница не
   дала создать `/tmp/flutter_tools.`, так что все его восемь находок получены
   чтением кода.

   Три предыдущих ревью находили по 18, 43 и 44 находки на зелёном гейте,
   причём третье — шесть P1, каждый из которых был тем же дефектом в соседней
   реализации. Четвёртое подтвердило правило: **все 52 его находки получены на
   зелёном гейте**, и ни одна им не ловится.

8. ~~**Заменить журнал наблюдателем — `ScopeConfig.observer`.**~~ **Сделано
   2026-08-19 и влито в `main` мержем `081c972`** (ветка `observer` удалена),
   пять задач плана
   `2026-08-19[2]-scope-observer-plan.md`:

   - `cc59355` — каркас: `ScopeObservable`, `ScopePhase`, `ScopeObserver`,
     `ScopeConfig.observer`, `notifyObserver` с `try`/`catch` и защитой от
     рекурсии, и пара `onInit`/`onDisposed` в `ScopeWidgetElementBase`,
     сделавшая видимыми синхронные семейства;
   - `348dbc6` — 26 точек журнала асинхронного скоупа переехали на хуки;
   - `0e1cada` — 11 точек контейнера зависимостей, 2 точки одной зависимости
     и 7 точек `runStreamGuarded`;
   - `a852b93` — ломающий: `ScopePrintObserver`, удаление
     `scope_logger.dart` и девяти публичных имён, `logger_builder` из
     `pubspec.yaml` и из трёх примеров;
   - `5f1987e` — фазы отказа печатаются английской прозой, а не именем
     значения `ScopePhase`;
   - плюс `b6ee0ba`, `843fb8a`, `7d631ee` (документация задачи 5: тема
     `debug` переписана, `README.md` и оба `example/*/README.md` с зеркалами,
     `docs/architecture.md`, шапки обеих записей и `CHANGELOG.md`), `1fa18aa`
     и `d23b43c` (правки по ревью задачи 5: раздел 0.10.0 `CHANGELOG.md`
     очищен от API, которое этот же релиз удаляет).

   Перечислена работа, а не все коммиты ветки: те, что только проставляют
   в этих же документах номера предыдущих, сюда не идут — иначе список
   пришлось бы дополнять каждый раз, когда его дополняешь. Полный список —
   `git log --oneline main..observer`.

   Тестов стало **392** (было 384 на старте работы). Дизайн выполнен целиком;
   разошлось одно место — §4 обещала, что базовую пару `onInit`/`onDisposed`
   можно поставить так, чтобы событие не задваивалось, а `LiteScopeElementBase`
   наследует `AsyncScopeElementBase`, поэтому точка постановки ничего не
   решала; развёл их переключатель
   `ScopeWidgetElementBase.reportsOwnLifecycle`. Это записано в шапке спеки.

   **Переход на `logger_builder` 0.7.0 отменён** этим решением: зависимость
   ушла, ждать публикации незачем. Замер 2026-08-19 показал, что переход был
   бы дешёвым (384/384, `analyze` чист, пол не двигается, кода менять не надо),
   и он же дал главный довод против самой зависимости: ломающая правка 0.7.0 —
   пин публикатора по уровню переживает общий сеттер — меняет поведение
   публичного API scopo, хотя scopo не меняет ни строки. Подробности — в §1
   спеки.

   Запись владельца в `docs/backlog.md` («Стоит ли нам дальше использовать
   logger_builder…») закрыта вместе с работой и удалена — см. «С чего начать»,
   п. 5.

9. ~~**Провал `init()` невидим наблюдателю.**~~ **Сделано 2026-08-20.**
   Разбор и то, чем запись о находке ошибалась, — в «Известные проблемы»,
   раздел «Открыто, вне списка ревью». Коротко: невидим он был обоим видам
   семейств, потому что синхронный хук `init()` у них общий, а фаза
   асинхронного семейства стартует только при `_didInit`; правка одна, в
   `catch` внутри `ScopeWidgetElementBase.build()`. Тестов 397 → 398. Попутно
   найдена смежная — отказ `_releaseController` невидим наблюдателю, — и она
   закрыта следом, в тот же день, а за ней — и третья, отказы разбора
   `ScopeCore` вместе с `unmountScope()` в `unmount()` элемента. Тестов
   397 → 402, всего правок видимости четыре. Разбор каждой — в «Известные
   проблемы», раздел «Открыто, вне списка ревью»; **у трёх из четырёх находка
   оказывалась шире, чем была записана**, и каждый раз это показывал падающий
   тест, а не чтение. На четвёртой поиск по соседям сошёлся: **все
   пользовательские точки жизненного цикла теперь отчитываются наблюдателю**
   — `create`, `onMount`, `init`, `initScope`/`initData`/`initDependencies`,
   `onUnmount`, `disposeScope`, `dispose` зависимости, контроллера и
   `ScopeModel`. ~~Осталась одна семья, и она граница, а не пробел: `buildOn*`
   (см. ниже).~~ Та одна семья **закрыта 2026-08-22**, п. 14: с
   `ScopePhase.build` отчитывается и сборка, и список пользовательских точек
   сошёлся целиком.

10. **Волна 16, блок таймаутов — сделано 2026-08-21**, коммит `1efd619`.
    Четыре находки четвёртого ревью одной работой: C1, H8, M7, M8 — весь P0
    плана. Разбор — в «Известные проблемы», раздел «Из ревью», и в вердиктах
    самого ревью. Коротко, и это главное, что стоит унести дальше:

    - **резолвер — единственная точка, где лимит становится лимитом, и её
      обходят.** Шесть ограниченных ожиданий пакета, пять шли через
      `resolveTimeout`, шестое (`ScopeAutoDependencies._disposeBounded`) читало
      `ScopeConfig.defaultDisposeScopeTimeout` напрямую. Найти это можно было
      только пересчётом всех шести — ни один тест не краснел. Поэтому
      `assert(!isNegative)` теперь стоит и в обоих резолверах, и в трёх местах,
      где `Duration` уходит в `Zone.root.createTimer`: у самого таймера он
      мёртвый по построению, и оставлен именно как охрана инварианта, который
      один раз уже обошли;
    - **вся поверхность `Duration` в пакете перечислима грепом, и это сильнее
      мутации.** Четыре таймаута × пять семейств, `pauseAfterInitialization` ×
      пять семейств, четыре умолчания `ScopeConfig`, два
      `waitForChildren(timeout:)` — и ни одного `Duration` в `utils/`. Резолвер
      обходили ровно два слота, и оба закрыты;
    - **выбор из двух путей C1 решается тем, что код уже делает.** Ревью
      предлагало либо снимать маркер с `fallback`, либо ассертить и его
      (превратив четыре поля `ScopeConfig` в сеттеры). Три умолчания из четырёх
      **уже** снимали маркер, потому что `resolveTimeout` проверяет `own ??
      fallback` целиком, — так что второй путь означал бы запретить работающее.
      Прежде чем выбирать из предложенного, стоит посмотреть, что из этого уже
      сделано;
    - **отказ отдельному скоупу и отказ всему приложению — разные решения.**
      `initCancellationTimeout` отвергает `ScopeTimeout.none` ассертом именно
      затем, чтобы безлимитная отмена оставалась решением всего приложения;
      значит на глобальном умолчании она обязана быть принимаемой. Ассерт на
      обоих концах убил бы саму причину, по которой ассерт есть на одном;
    - **`pauseAfterInitialization` — не таймаут, и это видно только по
      смыслу.** Он стоит вплотную к четырём таймаутам, тот же `Duration?`, и
      маркер туда присваивался молча. Ассерт на него поставлен **до** проверки
      `ScopeConfig.pauseAfterInitializationEnabled`: иначе сьюта, выключившая
      все паузы, выключала бы и единственное, что говорит о неверном значении.

11. **Волна 17, блок продолжения уборки — сделано 2026-08-21**, коммиты
    `bd58caa` (H1+H13), `9f5b522` (H3), `7c10b8f` (H2), `9bb8ab9` (H9+H10).
    Шесть находок — **весь P1 плана**. Что стоит унести дальше:

    - **у `unmount()` элемента нет вызывающего, которому можно бросить.**
      Это `BuildOwner._inactiveElements._unmountAll()` — цикл без границы
      вокруг элемента, по списку, который он уже очистил. Бросок оттуда стоит
      разбора **каждому** скоупу партии за упавшим. Мерилом было трое сиблингов
      в `Column`: разобрались двое;
    - **тест может держать ровно тот дефект, который находка называет
      дефектом.** `'keeps the failure of the state when the dependencies fail
      to unmount behind it'` требовал, чтобы первый отказ приходил из
      фреймворка, а второй от scopo. Пришлось переписать. Прежде чем считать
      зелёный тест доказательством, стоит посмотреть, что именно он держит;
    - **находка бывает права о дефекте и неточна о пути.** H13 обычным путём не
      проявляется — скоуп сам зовёт `performUnmount()` раньше, и внутри
      `performDispose` он no-op. Две стадии встречаются впервые на пути
      провалившейся инициализации. Существующий зелёный тест на «обычном» пути
      этого не показывал и показать не мог;
    - **третий случай находки бывает закрыт уже порядком, и это видно только
      пробой.** У H3 экран закрытия, падающий на монтировании, `try` в сборке
      не ловит — но `ScreenshotReplacer` стоит первым ребёнком `Stack`, поэтому
      барьер уже отпущен. Отдельная граница ошибок, которую предлагало
      направление, не понадобилась; вместо неё написан тест, держащий порядок
      детей;
    - **два читателя одного флага могут проверять разное.** У H2 `build()`
      спрашивал кэш, `updateChild` — нет. Чинить надо было флаг, а не второго
      читателя;
    - **«идёт прямо сейчас» — факт, который негде прочесть.** У H10 дерево
      зависимостей сидит в `ScopeDependencyInitial` весь прогон и выходит из
      него в самом конце, поэтому все проверки «уже инициализировано» второй
      параллельный `init()` пропускают. Тот же класс на стороне разбора нашёлся
      пробой и **не закрыт** — см. «Открыто, вне списка ревью»;
    - **публичный `sealed` — довод против нового состояния.** Новый вариант
      `ScopeDependencyState` сломал бы все исчерпывающие `switch` у
      потребителей ради факта, который снаружи не нужен. Приватный флаг.

12. **Волна 18, блок P2 — сделано 2026-08-21**, коммиты `ac037f3` (H11),
    `8916128` (H12+H14), `b640b67` (H5+H4), `3e56d58` (H7), `ddf3f25` (H6).
    Семь находок; вместе с волнами 16 и 17 закрыт весь блок High и Critical.
    Что стоит унести дальше:

    - **направление находки бывает неверным, и проверяется это замером.** У H11
      предложенный `IsBuildingExtension.isBuilding` не расширил бы ассерт, а
      выключил его: он поднят на весь `persistentCallbacks`, то есть и на
      `didChangeDependencies`. Различает три состояния только
      `RenderObject.debugActiveLayout`. Четыре точки одного кадра — в вердикте
      H11;
    - **обещание бывает не «недоделанным», а невыполнимым.** У H14 гейт
      ready-ветки поставить некуда: состояние создаёт **сама** ready-ветка, так
      что ждать её инициализации ей нечем. Прежде чем выбирать из двух путей
      находки, стоит проверить, существует ли второй;
    - **охрана без пары проверяется наполовину.** У H12 тест «хук не позвали»
      зелен и у кода, который не зовёт хук никогда, — поэтому тестов два;
    - **один лимит вокруг двух стадий — это лимит, который пропускает вторую.**
      У H5 зависшее `disposeStateAsync` съедало весь `disposeScopeTimeout`, и
      сдавались обе половины сразу. Разведено переключателем
      `boundsDisposeScopeItself` — пара к `reportsOwnLifecycle`, заведённая по
      тому же поводу: семейство знает о себе то, чего общий разбор знать не
      может;
    - **сдаться со снимком не значит сдаться с подменой.** У H4
      `ScreenshotReplacer`, отчаявшись снять кадр, оставлял живого ребёнка, — а
      снимка ждут ровно затем, чтобы этого ребёнка отпустить;
    - **у Flutter нет признака «моя ветка не показывается».** Замер H6:
      скрытая ветка `IndexedStack` и поддерево под `Offstage` отвечают на
      `TickerMode` и `ModalRoute` ровно то же, что показанные. Поэтому у
      `NavigationNode` появился `enabled` — это решение владельца, и объяснение
      «зачем и почему» он попросил положить в `README.md` отдельным разделом;
    - **контрольный тест, зелёный всегда, тоже бывает нужен.** У H6 такой
      держит поведение, ради которого параметр и заведён: без `enabled` одно
      нажатие разматывает оба стека. Без него причина появления параметра
      исчезла бы из сьюты.

13. **Волны 19–25 — сделано 2026-08-21 и 2026-08-22**, по решению владельца
    «чиним всё до публикации». Тридцать пять находок: 19 — публичная
    поверхность (`bef7a44`), 20 — контейнер зависимостей (`e21b439`), 21 — учёт
    пересборок (`46f316b`), 22 — навигация (`785ce40`), 23 — обещания и
    диагностика (`b3a9b02`), 24 — наблюдатели, подписки, архив (`83860ab`),
    25 — отчётность разбора (`88f11ce`). Что стоит унести дальше:

    - **тест, держащий дефект, встретился шесть раз за одну волну.** У L1 все
      шесть тестов, написанных вокруг `runZonedGuarded`, держали именно тот
      канал, который находка называет неверным. То же у H1, L4, L9 и L5. Прежде
      чем считать зелёный тест доказательством, стоит спросить, что именно он
      держит;
    - **чужой `FlutterError.onError` глотает провал собственного теста, и
      прогон виснет, а не краснеет.** Наступил на это в волне 25: один прогон
      висел десять минут. Обработчик возвращают **до** проверок, а не только
      тир-дауном. Грабля была записана — и всё равно сработала;
    - **`git checkout -- <файл>` в цикле мутаций стирает правку.** Тоже
      записанная грабля, тоже сработала: цикл по охранам `NavigationNode` снёс
      готовое исправление M4. Для мутаций — копия файла в scratchpad и `cp`
      обратно;
    - **направление находки бывает неверным, и это видно только замером.**
      У H11 предложенный `isBuilding` выключил бы ассерт; у M6 релизная
      предпроверка не пишется вовсе (`RenderObject.layer` — `@protected`), и
      чинить пришлось не симметрию, а сам отчёт;
    - **две взаимно-избыточные охраны тестом не закрыть.** У M12 каждая делает
      другую недостижимой: убить можно любую, но не обе. Оставлены обе, причина
      написана над той, что стала недостижимой;
    - **`shouldNotify` отвечает на «надо ли будить», а не на «случилось ли»**
      (L4). Имя хука было право, код — нет;
    - **порядок отчётов обратен порядку отказов** (L1): второй отчитывается
      сразу, первый ждёт конца разбора, потому что он тот, который разбор
      несёт.

14. ~~**Отказ `buildOn*` до наблюдателя не доходит.**~~ **Сделано 2026-08-22**
    по решению владельца — `e7b01c7`, `ScopePhase.build`. Не волна и не находка
    ревью: это была запись «Открыто, вне списка ревью», помеченная как вопрос к
    владельцу. Дизайн — `docs/records/2026-08-22[1]-build-phase-design.md`,
    разбор и то, что стоит унести дальше, — в «Известные проблемы», раздел
    «Открыто, вне списка ревью». Коротко: охрана одна, в
    `ScopeWidgetElementBase.build()`, потому что `buildChild()` во всём пакете
    зовут из одной строки; отчёт **добавлен** к броску, а не поставлен на его
    место, потому что у сборки, в отличие от разбора без вызывающего, адресат
    есть; тестов два, и второй держит не покрытие, а выбор места охраны.
    Тестов 449 → **451**.

Следующая волна — 26-я, если будет что закрывать. Правила прежние: **у каждой проработанной
находки — вердикт абзацем в конце неё, в самом файле ревью**
(`AGENTS.md` §4); **закрыл находку — найди её же в соседней реализации**, и
результат поиска пиши в отчёт волны. Третье ревью добавило к этому: **соседа
ищи мутацией, а не глазами** — шесть его P1 были именно такими пропущенными
соседями, и все шесть нашлись запуском кода, а не чтением.

Каждая волна закрывается по правилу, выведенному ревью: **найденное
исправление ищется в соседних реализациях** (последовательная/параллельная
группа, синхронная/асинхронная половина разбора, три семейства с одинаковыми
полями), и результат поиска пишется в отчёт волны. У каждой закрытой находки
ревью — вердикт в конце неё, в самом файле ревью (`AGENTS.md` §4).

~~**В бэклоге владельца три записи.**~~ **Все три закрыты 2026-08-18**;
оставлены здесь, чтобы было видно, чем именно:

- «время, когда можно переименовать переменные, колбэки и т.п.» — расширена
  владельцем просьбой посмотреть, нет ли ошибок и несогласованности в именах.
  Это решение владельца, а не работа: переименования публичной поверхности
  ломающие, и окно для них — до 1.0. Практически это блок типов волны 14: P1-5
  всё равно ломающий, так что переименования в тот же релиз стоят ноль сверху;
- «обновить `logger_builder`» — дописана владельцем 2026-08-17, в тот же день,
  когда он выпустил 0.6.0 и 0.6.1. В `pubspec.yaml` стоит `^0.5.0`. **С
  подъёмом `sdk` эта запись больше не сцеплена** (0.6.1 объявляет `^3.6.0`) —
  см. пункт 4 списка выше и «Тулчейн и окружение»;
- «поднять `sdk` до `^3.7.0`» — записана агентом по просьбе владельца
  2026-08-17, после разбора, соответствует ли объявленный Dart полу Flutter.
  Не соответствует и не может: подробности и измерения — в «Тулчейн и
  окружение». Делать отдельно от волны 14.

Четыре другие записи закрыла волна 12 — ссылки на темы из `README.md`, разделы
«где можно ошибиться» в темах `AsyncScope`, `AsyncDataScope`, `Scope` и
`ScopeModel`, раздел про progress в теме `AsyncScope`, формулировка про
`close()`. Вычеркнуты из бэклога по просьбе владельца, каждая перед этим
проверена по коду и документации.

Запись про правило `unmount`/`onUnmount` закрыта волной 14 (`19ecde3`, раздел
темы `Scope` про две инициализации) и вычеркнута по просьбе владельца.

Ещё одну владелец дописал по ходу волны 13 — «в примерах AsyncScope и
AsyncDataScope что будет с connection/database при отмене?» — и она же её и
закрыла: утекало, разбор в отчёте `2026-08-17[2]`.

## Как здесь работают

Полный регламент — в `AGENTS.md`. Коротко: тест до исправления и проверка
нагруженности откатом (мутацией); `docs/handoff.md` обновляется по ходу, а не в
конце; у каждой проработанной находки ревью — вердикт абзацем в конце неё, в
самом файле ревью; выполненное удаляется из `docs/backlog.md`; мерж прямо в
`main`; чужие правки в коммит не попадают; правка оригинала в `doc/` не заканчивается без
правки зеркала в `docs/ru/` и `sh docs/ru/stamp.sh`; перед мержем прогоняются
восемь команд из раздела 6 `AGENTS.md` (через `fvm`, тулчейн 3.27.0).
